import { describe, test } from "vitest";

describe("research workspace init flow", () => {
  test.todo(
    "creates the fixed workspace contract under researcher/researches/<slug>/ without accepting user-defined layout overrides",
  );
  test.todo(
    "writes brief.md, manifest.json, and sources.json plus the required insights/, data/, analysis/, and reports/ directories in one deterministic operation",
  );
  test.todo(
    "seeds stable IDs and machine-readable status fields without coupling the implementation to Next.js routes or runtime-specific adapters",
  );
  test.todo(
    "uses deterministic filesystem writers and shared temp-workspace helpers instead of ad hoc prompt-owned JSON mutation",
  );
});
