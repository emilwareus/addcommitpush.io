---
phase: 05-status-freshness-verification
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - internal/tools/researcher/core/status/summary.ts
  - internal/tools/researcher/core/status/debt.ts
  - scripts/research-status.ts
  - internal/tools/researcher/__tests__/status-summary.spec.ts
autonomous: true
requirements:
  - RSCH-02
  - STAT-01
  - STAT-02
user_setup: []
must_haves:
  truths:
    - "A user can inspect one deterministic status summary that shows freshness debt, verification debt, impacted IDs, and one primary next action."
    - "Unsupported-claim and verification-debt checks stay file-based and deterministic rather than semantic or model-judged."
    - "The runtime entrypoint remains thin and does not bypass shared summary/debt logic."
  artifacts:
    - "internal/tools/researcher/core/status/summary.ts and debt.ts provide one canonical status evaluation path."
    - "scripts/research-status.ts exposes that evaluation through a thin JSON-only CLI."
    - "internal/tools/researcher/__tests__/status-summary.spec.ts proves routing, debt aggregation, and CLI behavior."
  key_links:
    - "status evaluation reuses canonical artifacts and sources from disk rather than hidden in-memory state."
    - "nextRecommendedAction extends the existing resume/router model instead of inventing a second routing system."
    - "manifest aggregate counts stay in sync with the derived detailed debt view."
---

<objective>
Build the read-side status and debt evaluator that operators use to inspect a research and decide what to do next.

Purpose: Deliver RSCH-02, STAT-01, and the read-side half of STAT-02 through one status summary
service plus a thin `/research-status` CLI.
Output: shared status/debt evaluators, a thin runtime wrapper, and focused summary tests.
</objective>

<context>
@.planning/phases/05-status-freshness-verification/05-CONTEXT.md
@.planning/phases/05-status-freshness-verification/05-RESEARCH.md
@.planning/phases/05-status-freshness-verification/05-VALIDATION.md
@.planning/phases/05-status-freshness-verification/05-01-PLAN.md
@internal/tools/researcher/core/resume.ts
@internal/tools/researcher/core/artifacts/markdown.ts
@internal/tools/researcher/core/reports/lineage.ts
@internal/tools/researcher/core/sources/store.ts
@researcher/WORKFLOWS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic status summary and verification-debt evaluation</name>
  <files>internal/tools/researcher/core/status/summary.ts, internal/tools/researcher/core/status/debt.ts, internal/tools/researcher/__tests__/status-summary.spec.ts</files>
  <behavior>
    - Test 1: a healthy research returns clear freshness debt, clear verification debt, and a stage-appropriate next action.
    - Test 2: stale sources outrank downstream review actions and produce grouped impacted IDs for insights, analyses, and reports.
    - Test 3: unsupported or contradictory upstream conditions surface verification debt and a debt-resolution next action.
  </behavior>
  <action>Create a status summary service that loads canonical files, computes aggregate freshness/verification debt, groups impacted IDs by layer, and emits one primary `nextRecommendedAction`. Reuse the existing resume priority model, but extend it with blocking verification-debt handling after freshness. Keep unsupported/debt detection deterministic and grounded in actual artifact lineage plus explicit contradiction/open-question data.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/status-summary.spec.ts` passes with healthy, stale, and verification-debt scenarios plus grouped impacted-ID assertions.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/status-summary.spec.ts</automated>
  </verify>
  <done>Operators can inspect one decisive status summary without manually traversing the artifact graph.</done>
</task>

<task type="auto">
  <name>Task 2: Add the thin `research-status` runtime entrypoint</name>
  <files>scripts/research-status.ts, internal/tools/researcher/__tests__/status-summary.spec.ts</files>
  <behavior>
    - Test 1: the CLI accepts `--project-root` and `--slug` and prints deterministic JSON for the derived status summary.
    - Test 2: missing or invalid workspace inputs fail hard rather than emitting partial status.
  </behavior>
  <action>Add `scripts/research-status.ts` as a JSON-only wrapper over the shared status summary service. Keep all routing and debt logic in core modules. Do not embed fallback prompting, dashboard rendering, or alternate output modes in this plan.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/status-summary.spec.ts` passes with CLI smoke coverage and deterministic JSON assertions.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/status-summary.spec.ts</automated>
  </verify>
  <done>Status inspection has a runtime-neutral entrypoint that stays thin and deterministic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical files -> status summary | Read-side logic must not misstate debt or impacted IDs due to partial graph traversal. |
| status output -> operator routing | One primary next action must not mask blocking debt behind weaker stage recommendations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | R | `internal/tools/researcher/core/status/summary.ts`, `debt.ts` | mitigate | Derive all debt and next-action output from canonical on-disk artifacts with deterministic priority rules. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/status-summary.spec.ts`
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `rg -n "nextRecommendedAction|verificationDebt|impacted" internal/tools/researcher/core/status/summary.ts internal/tools/researcher/core/status/debt.ts scripts/research-status.ts`
</verification>

<output>
After completion, create `.planning/phases/05-status-freshness-verification/05-02-SUMMARY.md`
</output>
