---
phase: 03-insights-analysis-graph
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - internal/tools/researcher/core/analysis/backlinks.ts
  - internal/tools/researcher/core/analysis/upsert.ts
  - internal/tools/researcher/__tests__/analysis-upsert.spec.ts
  - internal/tools/researcher/__tests__/resume.spec.ts
  - scripts/research-analysis.ts
autonomous: true
requirements:
  - INS-03
  - INS-04
user_setup: []
must_haves:
  truths:
    - "A user can synthesize multiple insights into one canonical `ANL-*` Markdown artifact with a stable manifest-backed ID."
    - "Analysis artifacts keep contradictions, caveats, and open questions explicit as first-class structured sections instead of burying them in narrative prose."
    - "Insight backlinks and disk-only resume stay correct after Phase 3 analysis artifacts land on disk."
  artifacts:
    - "internal/tools/researcher/core/analysis/upsert.ts provides deterministic create/update behavior for canonical analysis artifacts."
    - "internal/tools/researcher/core/analysis/backlinks.ts reconciles each referenced insight’s `linked_analysis` array from the analysis artifact’s forward lineage."
    - "scripts/research-analysis.ts and internal/tools/researcher/__tests__/resume.spec.ts prove the runtime entrypoint and resume compatibility for Phase 3 analysis artifacts."
  key_links:
    - "internal/tools/researcher/core/analysis/upsert.ts reuses the existing Phase 2 store plus Phase 3 parser contracts rather than introducing notebook-first or provider-specific state."
    - "internal/tools/researcher/core/analysis/backlinks.ts updates insight Markdown frontmatter through the same shared artifact parser used by insight creation, keeping `linked_analysis` authoritative and sorted."
    - "internal/tools/researcher/__tests__/analysis-upsert.spec.ts and internal/tools/researcher/__tests__/resume.spec.ts prove contradiction-aware analysis creation, insight backlink reconciliation, and resume inventory routing."
---

<objective>
Add higher-order analysis artifacts with explicit contradiction tracking and insight backlinks, then prove Phase 3 still resumes correctly from disk.

Purpose: Deliver INS-03 and INS-04 by turning grouped insights into canonical `ANL-*` Markdown synthesis artifacts while keeping contradictions, caveats, and unresolved questions explicit and machine-readable.
Output: Shared analysis upsert and backlink helpers, contradiction-aware analysis tests, resume compatibility coverage, and a thin `research-analysis` CLI.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-insights-analysis-graph/03-CONTEXT.md
@.planning/phases/03-insights-analysis-graph/03-RESEARCH.md
@.planning/phases/03-insights-analysis-graph/03-VALIDATION.md
@.planning/phases/02-source-registry-evidence-capture/02-03-SUMMARY.md
@internal/tools/researcher/contracts/manifest.ts
@internal/tools/researcher/contracts/sources.ts
@internal/tools/researcher/core/resume.ts
@internal/tools/researcher/core/sources/store.ts
@internal/tools/researcher/fs/workspace-paths.ts
@scripts/research-insight.ts

<interfaces>
Planned outputs from `03-01` and `03-02` to build against:

```typescript
export interface AnalysisFrontmatter {
  id: string;
  title: string;
  status: "draft" | "validated" | "disputed" | "superseded";
  confidence: "high" | "medium" | "low" | "unknown";
  derived_from_insights: string[];
  tags: string[];
  linked_reports: string[];
  transitional_scaffold: boolean;
  created_at: string;
  updated_at: string;
}

export interface ParsedAnalysisDocument {
  frontmatter: AnalysisFrontmatter;
  sections: {
    question: string;
    synthesis: string;
    contradictions: string[];
    caveats: string[];
    openQuestions: string[];
    nextMoves: string[];
  };
}

export interface UpsertInsightResult {
  operation: "created" | "updated";
  insightId: string;
  path: string;
  sourceIds: string[];
}
```

Existing resume contract that must stay valid:

```typescript
export interface ResumeResearchResult {
  inventory: {
    sources: number;
    insights: number;
    analysis: number;
    reports: number;
  };
  nextRecommendedAction: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement contradiction-aware analysis upsert and insight backlink sync</name>
  <files>internal/tools/researcher/core/analysis/backlinks.ts, internal/tools/researcher/core/analysis/upsert.ts, internal/tools/researcher/__tests__/analysis-upsert.spec.ts, scripts/research-analysis.ts</files>
  <read_first>.planning/phases/03-insights-analysis-graph/03-CONTEXT.md, .planning/phases/03-insights-analysis-graph/03-RESEARCH.md, .planning/phases/03-insights-analysis-graph/03-VALIDATION.md, internal/tools/researcher/core/sources/store.ts, internal/tools/researcher/core/resume.ts, internal/tools/researcher/fs/workspace-paths.ts, internal/tools/researcher/__tests__/insight-upsert.spec.ts</read_first>
  <behavior>
    - Test 1: creating a new analysis allocates the next `ANL-*`, writes `analysis/<ANL-ID>-<slug>.md`, increments `manifest.inventory.analysis`, and appends the analysis ID to each referenced insight’s `linked_analysis` array.
    - Test 2: the analysis contract rejects missing `Contradictions`, `Caveats`, or `Open Questions` sections, and rejects fewer than two `derived_from_insights` entries unless `transitional_scaffold: true` explicitly invokes the D-40 exception.
    - Test 3: updating an analysis preserves its stable ID, reconciles removed and added insight backlinks, and keeps raw source URLs out of machine lineage by treating insight IDs as the only authoritative upstream references.
  </behavior>
  <action>Per D-37 through D-42, create `internal/tools/researcher/core/analysis/upsert.ts` and `internal/tools/researcher/core/analysis/backlinks.ts` on top of the shared Markdown parser from `03-01` and the existing Phase 2 source store for manifest-counter reuse. Resolve every referenced `INS-*` to an existing insight file, parse it through the same artifact boundary, and reconcile each insight’s `linked_analysis` array by diffing the previous and next analysis input set. Require the canonical body sections from D-39, and make `Contradictions`, `Caveats`, `Open Questions`, and `Next Moves` machine-readable as bullet lists where an explicit `- None noted yet.` entry is allowed but omission is not. Keep analyses Markdown-first per D-37, insight-first per D-38, and higher-order per D-40 by requiring at least two inputs unless `transitional_scaffold: true`; if that scaffold path is used, keep it explicit in frontmatter and do not bypass the required contradiction/caveat/open-question sections. Add `scripts/research-analysis.ts` as a thin JSON-only wrapper that accepts `--project-root`, `--slug`, optional `--analysis-id`, `--title`, repeated `--insight-id`, `--status`, `--confidence`, repeated `--tag`, optional `--transitional-scaffold`, `--question`, `--synthesis`, repeated `--contradiction`, repeated `--caveat`, repeated `--open-question`, and repeated `--next-move`, then prints deterministic JSON. Do not add report writes, stale propagation, notebook-as-canonical behavior, or provider-specific analysis logic.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/analysis-upsert.spec.ts internal/tools/researcher/__tests__/analysis-contract.spec.ts` passes with assertions for ID allocation, minimum insight-set enforcement, required contradiction/caveat/open-question sections, insight backlink add/remove reconciliation, and deterministic CLI output, and `rg -n "next_ids\\.analysis|inventory\\.analysis|linked_analysis|transitional_scaffold|Contradictions|Open Questions|JSON\\.stringify" internal/tools/researcher/core/analysis/upsert.ts internal/tools/researcher/core/analysis/backlinks.ts scripts/research-analysis.ts` shows the required behavior.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/analysis-upsert.spec.ts internal/tools/researcher/__tests__/analysis-contract.spec.ts</automated>
  </verify>
  <done>Higher-order analysis is deterministic, contradiction-aware, insight-first, and synchronized back into the underlying insight artifacts without adding new canonical state stores.</done>
</task>

<task type="auto">
  <name>Task 2: Extend resume coverage for Phase 3 artifact inventory and routing</name>
  <files>internal/tools/researcher/__tests__/resume.spec.ts</files>
  <read_first>internal/tools/researcher/core/resume.ts, internal/tools/researcher/__tests__/resume.spec.ts, internal/tools/researcher/__tests__/analysis-upsert.spec.ts</read_first>
  <action>Extend `internal/tools/researcher/__tests__/resume.spec.ts` with a Phase 3 fixture that includes canonical insight and analysis Markdown artifacts produced by the new contracts, then prove `resumeResearchWorkspace()` still reports correct `sources`, `insights`, and `analysis` inventory counts and keeps next-action routing aligned with the existing disk-only rules. Cover at least one case where a research in `extract` stage with sources, insights, and analysis on disk recommends `package-report`, and one case where stale source debt still wins over downstream analysis presence and recommends `refresh-sources`. Keep this as coverage only unless the tests expose a real resume bug; do not redesign resume around in-memory graph state or add Phase 5 freshness propagation here.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/resume.spec.ts` passes with Phase 3 artifact fixtures and assertions for inventory plus next-action routing, and `rg -n "package-report|refresh-sources|INS-0001|ANL-0001" internal/tools/researcher/__tests__/resume.spec.ts` shows the new Phase 3 resume cases.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/resume.spec.ts</automated>
  </verify>
  <done>Disk-only resume remains a trusted consumer of the filesystem after Phase 3 analysis artifacts land, with no hidden dependency on agent memory or a parallel graph index.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/operator analysis payload -> shared analysis core | Untrusted question text, contradiction lists, and insight IDs cross into stable analysis artifacts and backlink updates. |
| analysis forward lineage -> insight Markdown backlinks -> resume inventory | New analysis artifacts must not corrupt insight reuse links or break disk-only resume routing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | T | `internal/tools/researcher/core/analysis/upsert.ts`, `internal/tools/researcher/core/analysis/backlinks.ts` | mitigate | Require existing `INS-*` inputs, enforce D-39 section presence, keep contradiction/caveat/open-question lists explicit, and reconcile `linked_analysis` from forward lineage only through the shared artifact parser. |
| T-03-04 | R | `internal/tools/researcher/core/resume.ts`, `internal/tools/researcher/__tests__/resume.spec.ts` | mitigate | Prove canonical Phase 3 insight and analysis files still round-trip through disk-only resume inventory counting and next-action routing without adding hidden runtime state. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/analysis-upsert.spec.ts internal/tools/researcher/__tests__/analysis-contract.spec.ts`
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/resume.spec.ts`
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `rg -n "linked_analysis|transitional_scaffold|Contradictions|Open Questions|package-report|refresh-sources" internal/tools/researcher/core/analysis/upsert.ts internal/tools/researcher/core/analysis/backlinks.ts internal/tools/researcher/__tests__/resume.spec.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- New analyses allocate one stable `ANL-*` ID from the manifest-backed counter
- Analysis artifacts keep contradictions, caveats, and open questions explicit and machine-readable
- Insight backlinks stay synchronized with analysis references on create and update
- Resume still counts Phase 3 artifacts from disk and routes next actions correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-insights-analysis-graph/03-03-SUMMARY.md`
</output>
