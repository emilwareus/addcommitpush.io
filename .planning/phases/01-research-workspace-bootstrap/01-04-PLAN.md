---
phase: 01-research-workspace-bootstrap
plan: 04
type: execute
wave: 4
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - internal/tools/researcher/__tests__/resume.spec.ts
  - internal/tools/researcher/core/resume.ts
  - scripts/research-resume.ts
autonomous: true
requirements:
  - RSCH-03
user_setup: []
must_haves:
  truths:
    - "A user can reopen an existing research and recover working context from files alone."
    - "Resume trusts disk artifacts, not prior chat state, and validates manifest and source files before summarizing them."
    - "Resume surfaces current stage, open questions, artifact inventory, freshness debt, and a next recommended action from the actual workspace."
  artifacts:
    - "internal/tools/researcher/core/resume.ts loads and summarizes an existing workspace from disk."
    - "scripts/research-resume.ts exposes a thin runtime-neutral CLI for disk-only resume."
    - "internal/tools/researcher/__tests__/resume.spec.ts proves resume works against a real init-created workspace."
  key_links:
    - "internal/tools/researcher/core/resume.ts validates `manifest.json` and `sources.json` before computing the summary payload."
    - "internal/tools/researcher/core/resume.ts scans `insights/`, `analysis/`, and `reports/` instead of trusting stale manifest counts blindly."
    - "scripts/research-resume.ts consumes a workspace created by `scripts/research-init.ts`, catching contract drift between plans 01-03 and 01-04."
---

<objective>
Implement disk-only resume for an existing research and return a deterministic summary of its current state.

Purpose: Deliver RSCH-03 and prove the public Phase 1 workspace can be re-entered from files, not chat history.
Output: Resume core, resume integration tests, and a thin `scripts/research-resume.ts` entrypoint that summarizes the workspace.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-research-workspace-bootstrap/01-CONTEXT.md
@.planning/phases/01-research-workspace-bootstrap/01-RESEARCH.md
@.planning/phases/01-research-workspace-bootstrap/01-VALIDATION.md
@researcher/DESIGN.md
@researcher/ARTIFACT-MODEL.md
@researcher/WORKFLOWS.md
@internal/tools/researcher/contracts/manifest.ts
@internal/tools/researcher/contracts/sources.ts
@internal/tools/researcher/contracts/validators.ts
@internal/tools/researcher/fs/workspace-paths.ts
@internal/tools/researcher/core/init.ts
@scripts/research-init.ts

<interfaces>
Resume payload to create:

```typescript
type ResumeResearchResult = {
  researchId: string;
  slug: string;
  title: string;
  workspacePath: string;
  stage: string;
  state: string;
  openQuestions: string[];
  freshnessDebt: string;
  inventory: {
    sources: number;
    insights: number;
    analysis: number;
    reports: number;
  };
  nextRecommendedAction: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement disk-only resume and inventory-based summary logic</name>
  <files>internal/tools/researcher/__tests__/resume.spec.ts, internal/tools/researcher/core/resume.ts</files>
  <read_first>.planning/phases/01-research-workspace-bootstrap/01-CONTEXT.md, .planning/phases/01-research-workspace-bootstrap/01-RESEARCH.md, researcher/DESIGN.md, researcher/ARTIFACT-MODEL.md, researcher/WORKFLOWS.md, internal/tools/researcher/contracts/validators.ts, internal/tools/researcher/fs/workspace-paths.ts, internal/tools/researcher/core/init.ts, internal/tools/researcher/__tests__/resume.spec.ts</read_first>
  <behavior>
    - Test 1: resume loads a workspace created by the Phase 1 init flow and returns `researchId`, `slug`, `title`, `stage`, `state`, `openQuestions`, inventory counts, and `nextRecommendedAction`.
    - Test 2: resume scans `insights/`, `analysis/`, and `reports/` from disk so the summary reflects actual files even if manifest counters are stale.
    - Test 3: missing `brief.md`, invalid `manifest.json`, invalid `sources.json`, or traversal slugs fail hard instead of guessing.
  </behavior>
  <action>Create `internal/tools/researcher/core/resume.ts` to deliver RSCH-03 per D-04 and D-10 through D-13. Replace the Wave 0 placeholders in `resume.spec.ts` with real assertions, resolve the bounded workspace path, load `brief.md`, validate `manifest.json` and `sources.json`, scan `insights/`, `analysis/`, and `reports/` directories from disk, and compute a deterministic `nextRecommendedAction` from manifest stage plus actual inventory. Keep the summary compact and machine-readable; do not add source-harvest, insight-promotion, or report-generation behavior, and do not consult prior chat state or hidden continuation files.</action>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/resume.spec.ts</automated>
  </verify>
  <done>A Researcher workspace can be reconstructed from durable artifacts alone and summarized into deterministic machine state.</done>
</task>

<task type="auto">
  <name>Task 2: Add the thin `research-resume` CLI entrypoint and smoke test it against a real initialized workspace</name>
  <files>scripts/research-resume.ts</files>
  <read_first>internal/tools/researcher/core/resume.ts, internal/tools/researcher/core/init.ts, scripts/research-init.ts, researcher/WORKFLOWS.md</read_first>
  <action>Create `scripts/research-resume.ts` as a thin TypeScript entrypoint that accepts `--project-root` and `--slug`, resolves the workspace through the shared core, and prints a deterministic JSON summary containing `researchId`, `workspacePath`, `stage`, `inventory`, and `nextRecommendedAction`. Verify it against a workspace created by `scripts/research-init.ts` so plans 01-03 and 01-04 share one public contract. Keep the entrypoint runtime-neutral: no slash-command glue, no interactive prompts, and no fallback reads from chat history or hidden memory.</action>
  <verify>
    <automated>TMPDIR="$(mktemp -d)" && pnpm exec tsx scripts/research-init.ts --project-root "$TMPDIR" --slug deep-research-os --title "Deep Research OS" --question "How should research artifacts be structured for reuse?" >/dev/null && pnpm exec tsx scripts/research-resume.ts --project-root "$TMPDIR" --slug deep-research-os | rg -n '"nextRecommendedAction"|"inventory"|"workspacePath"'</automated>
  </verify>
  <done>Operators can resume an existing research from disk through a deterministic CLI that stays aligned with the init-created workspace contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv -> `internal/tools/researcher/core/resume.ts` | Untrusted input becomes filesystem lookups into the research workspace. |
| on-disk workspace -> resume summary | Structured files and directory contents become trusted summary data for later workflows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | T | `internal/tools/researcher/core/resume.ts` | mitigate | Reuse the shared workspace path resolver and reject traversal or root-confusion inputs before any read. |
| T-01-12 | D | `internal/tools/researcher/core/resume.ts` | mitigate | Validate `manifest.json` and `sources.json` before summarizing; abort on invalid machine state instead of attempting recovery. |
| T-01-13 | T | `internal/tools/researcher/core/resume.ts` | mitigate | Scan artifact directories from disk and do not trust stale counters alone, preventing poisoned manifest inventory from driving later flows. |
| T-01-14 | R | `scripts/research-resume.ts` | accept | The CLI only reports project-local state and does not persist mutations in Phase 1; repudiation impact is low once validation is enforced. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint`
- [ ] `pnpm typecheck`
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/resume.spec.ts`
- [ ] `TMPDIR="$(mktemp -d)" && pnpm exec tsx scripts/research-init.ts --project-root "$TMPDIR" --slug deep-research-os --title "Deep Research OS" --question "How should research artifacts be structured for reuse?" >/dev/null && pnpm exec tsx scripts/research-resume.ts --project-root "$TMPDIR" --slug deep-research-os | rg -n '"nextRecommendedAction"|"inventory"|"workspacePath"'`
- [ ] `rg -n "brief.md|manifest.json|sources.json|nextRecommendedAction" internal/tools/researcher/core/resume.ts scripts/research-resume.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Resume reconstructs workspace state from disk-only artifacts
- Resume rejects invalid or missing machine state instead of guessing
- The resume summary reports stage, inventory, freshness debt, and next recommended action for a real init-created workspace
</success_criteria>

<output>
After completion, create `.planning/phases/01-research-workspace-bootstrap/01-04-SUMMARY.md`
</output>
