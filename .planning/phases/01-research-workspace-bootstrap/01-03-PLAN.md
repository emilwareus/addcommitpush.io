---
phase: 01-research-workspace-bootstrap
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - internal/tools/researcher/__tests__/init.spec.ts
  - internal/tools/researcher/fs/write-json-atomically.ts
  - internal/tools/researcher/core/workspace-seed.ts
  - internal/tools/researcher/core/id.ts
  - internal/tools/researcher/core/init.ts
  - scripts/research-init.ts
autonomous: true
requirements:
  - RSCH-01
user_setup: []
must_haves:
  truths:
    - "A user can initialize a new research in one deterministic operation."
    - "The init flow always creates the fixed public workspace contract and never writes outside the Researcher root."
    - "New researches start with stable IDs, a seeded brief, and zeroed artifact inventory that later phases can trust."
  artifacts:
    - "internal/tools/researcher/core/workspace-seed.ts creates `brief.md`, `manifest.json`, `sources.json`, `insights/`, `data/`, `analysis/`, and `reports/`."
    - "internal/tools/researcher/core/init.ts creates new researches under `researcher/researches/<slug>/`."
    - "scripts/research-init.ts exposes a thin runtime-neutral CLI entrypoint for the shared init core."
  key_links:
    - "internal/tools/researcher/core/init.ts imports workspace-seed and validator helpers from plan 01-02 instead of recreating layout logic."
    - "internal/tools/researcher/fs/write-json-atomically.ts protects `manifest.json` and `sources.json` from partial replacement."
    - "internal/tools/researcher/__tests__/init.spec.ts proves the CLI and core produce the public workspace contract exactly."
---

<objective>
Implement deterministic new-research initialization on top of the shared Phase 1 contract.

Purpose: Deliver RSCH-01 end to end so a bounded research can be created from files instead of ad hoc manual setup.
Output: Atomic JSON write helper, reusable workspace seed helper, stable ID allocation, core init service, integration tests, and a thin `scripts/research-init.ts` entrypoint.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-research-workspace-bootstrap/01-CONTEXT.md
@.planning/phases/01-research-workspace-bootstrap/01-RESEARCH.md
@.planning/phases/01-research-workspace-bootstrap/01-VALIDATION.md
@researcher/WORKFLOWS.md
@researcher/templates/RESEARCH.md
@internal/tools/researcher/contracts/manifest.ts
@internal/tools/researcher/contracts/sources.ts
@internal/tools/researcher/contracts/workspace.ts
@internal/tools/researcher/contracts/validators.ts
@internal/tools/researcher/fs/workspace-paths.ts
@internal/tools/researcher/__tests__/init.spec.ts

<interfaces>
Expected shared-contract exports from plan `01-02`:

```typescript
type ResearchWorkspacePaths = {
  projectRoot: string;
  researcherRoot: string;
  researchesRoot: string;
  workspaceRoot: string;
  briefPath: string;
  manifestPath: string;
  sourcesPath: string;
};

declare function resolveResearchWorkspacePaths(
  projectRoot: string,
  slug: string,
): ResearchWorkspacePaths;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement atomic workspace seeding and the core init flow against the existing init spec</name>
  <files>internal/tools/researcher/__tests__/init.spec.ts, internal/tools/researcher/fs/write-json-atomically.ts, internal/tools/researcher/core/workspace-seed.ts, internal/tools/researcher/core/id.ts, internal/tools/researcher/core/init.ts</files>
  <read_first>.planning/phases/01-research-workspace-bootstrap/01-CONTEXT.md, .planning/phases/01-research-workspace-bootstrap/01-RESEARCH.md, researcher/WORKFLOWS.md, researcher/templates/RESEARCH.md, internal/tools/researcher/contracts/workspace.ts, internal/tools/researcher/fs/workspace-paths.ts, internal/tools/researcher/contracts/validators.ts, internal/tools/researcher/__tests__/init.spec.ts</read_first>
  <behavior>
    - Test 1: a safe slug like `deep-research-os` creates `researcher/researches/deep-research-os/brief.md`, `manifest.json`, `sources.json`, `insights/`, `data/`, `analysis/`, and `reports/`.
    - Test 2: `initResearchWorkspace` seeds `research.id` as `RES-YYYYMMDD-<slug>`, sets `status.stage` to `intake`, and zeros `inventory.sources`, `inventory.insights`, `inventory.analysis`, and `inventory.reports`.
    - Test 3: an existing workspace directory or traversal slug like `../escape` throws a hard error before any write occurs.
  </behavior>
  <action>Replace the Wave 0 placeholders in `init.spec.ts` with real integration assertions, then implement `write-json-atomically.ts`, `workspace-seed.ts`, `id.ts`, and `init.ts` to make those tests pass. `write-json-atomically.ts` must use a temp-file-and-rename sequence so `manifest.json` and `sources.json` are never partially replaced. `workspace-seed.ts` must create exactly `brief.md`, `manifest.json`, `sources.json`, `insights/`, `data/`, `analysis/`, and `reports/` under the validated root and seed zeroed inventory counts. `init.ts` must accept explicit `projectRoot`, `slug`, `title`, `question`, optional `audience`, and optional `tags`, generate the `RES-YYYYMMDD-<slug>` ID, render `brief.md` from `researcher/templates/RESEARCH.md`, and call the shared seed helpers instead of reimplementing directory creation. Do not introduce runtime-specific command syntax, fallback paths, or user-defined layouts.</action>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/init.spec.ts</automated>
  </verify>
  <done>The shared core can create a valid Phase 1 research workspace safely, atomically, and without manual file creation.</done>
</task>

<task type="auto">
  <name>Task 2: Add the thin `research-init` CLI entrypoint</name>
  <files>scripts/research-init.ts</files>
  <read_first>internal/tools/researcher/core/init.ts, researcher/WORKFLOWS.md, researcher/templates/RESEARCH.md, package.json</read_first>
  <action>Create `scripts/research-init.ts` as a thin TypeScript entrypoint that parses `--project-root`, `--slug`, `--title`, `--question`, optional `--audience`, and repeatable `--tag` flags, then delegates directly to the shared init core. The script should print the created workspace path and research ID to stdout in a deterministic machine-readable JSON object. Keep the entrypoint runtime-neutral per D-11 through D-13: no slash-command glue, no prompt text, no Next.js coupling, and no package.json script churn beyond the repo-required `typecheck` already added in plan 01-01.</action>
  <verify>
    <automated>TMPDIR="$(mktemp -d)" && pnpm exec tsx scripts/research-init.ts --project-root "$TMPDIR" --slug deep-research-os --title "Deep Research OS" --question "How should research artifacts be structured for reuse?" | rg -n '"researchId"|"workspacePath"'</automated>
  </verify>
  <done>Operators have a deterministic CLI entrypoint for creating new researches that matches the shared Phase 1 contract exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv -> `internal/tools/researcher/core/init.ts` | Untrusted command input becomes filesystem paths and seeded manifest values. |
| init core -> workspace seed helpers | Seeded structured state becomes the canonical source of truth for later resume and status flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | T | `internal/tools/researcher/core/init.ts` | mitigate | Reuse the root-confined path resolver from plan 01-02 and reject unsafe slugs before any write. |
| T-01-08 | T | `internal/tools/researcher/fs/write-json-atomically.ts` | mitigate | Use temp-file writes plus rename for `manifest.json` and `sources.json`; stop execution if an atomic write cannot complete cleanly. |
| T-01-09 | D | `internal/tools/researcher/__tests__/init.spec.ts` | mitigate | Cover traversal, overwrite, and fixed-layout creation paths so invalid workspace inputs fail immediately. |
| T-01-10 | R | `internal/tools/researcher/core/id.ts` | accept | ID generation is deterministic from date plus slug and is not a security boundary by itself in Phase 1. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint`
- [ ] `pnpm typecheck`
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/init.spec.ts`
- [ ] `TMPDIR="$(mktemp -d)" && pnpm exec tsx scripts/research-init.ts --project-root "$TMPDIR" --slug deep-research-os --title "Deep Research OS" --question "How should research artifacts be structured for reuse?" | rg -n '"researchId"|"workspacePath"'`
- [ ] `rg -n "researcher/researches|status.stage|inventory" internal/tools/researcher/core/init.ts internal/tools/researcher/core/workspace-seed.ts internal/tools/researcher/__tests__/init.spec.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `scripts/research-init.ts` creates the fixed public workspace layout and nothing else
- The init flow refuses traversal slugs and existing-workspace overwrites
- New research manifests start with stable IDs, intake stage, and zeroed inventory counters
</success_criteria>

<output>
After completion, create `.planning/phases/01-research-workspace-bootstrap/01-03-SUMMARY.md`
</output>
