---
phase: 05-status-freshness-verification
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - internal/tools/researcher/core/freshness/propagation.ts
  - internal/tools/researcher/core/sources/refresh.ts
  - internal/tools/researcher/core/status/summary.ts
  - internal/tools/researcher/__tests__/freshness-propagation.spec.ts
  - internal/tools/researcher/__tests__/resume.spec.ts
autonomous: true
requirements:
  - STAT-02
  - STAT-03
  - STAT-01
user_setup: []
must_haves:
  truths:
    - "Source refresh propagates stale/fresh changes into dependent insights and reports through the existing lineage graph."
    - "Impacted analyses remain visible to operators even though analyses do not gain a persisted stale flag."
    - "Stale freshness debt still outranks downstream review actions in resume/status routing."
  artifacts:
    - "internal/tools/researcher/core/freshness/propagation.ts owns downstream stale/fresh recomputation."
    - "internal/tools/researcher/core/sources/refresh.ts invokes that propagation through the existing mutation boundary."
    - "internal/tools/researcher/__tests__/freshness-propagation.spec.ts and internal/tools/researcher/__tests__/resume.spec.ts prove propagation and routing priority."
  key_links:
    - "freshness propagation walks the existing source -> insight -> analysis/report graph instead of maintaining a parallel impact index."
    - "analysis impact is surfaced in read-side output, not persisted as a second stale-state layer."
    - "refresh recomputation clears stale flags only when upstream support is genuinely healthy again."
---

<objective>
Propagate freshness impact through the artifact graph and keep routing behavior correct once downstream impact becomes explicit.

Purpose: Finish STAT-02 and STAT-03 by wiring source refresh to downstream artifact health
recomputation and preserving debt-aware routing.
Output: shared freshness propagation logic, refresh integration, and focused propagation/routing
tests.
</objective>

<context>
@.planning/phases/05-status-freshness-verification/05-CONTEXT.md
@.planning/phases/05-status-freshness-verification/05-RESEARCH.md
@.planning/phases/05-status-freshness-verification/05-VALIDATION.md
@.planning/phases/05-status-freshness-verification/05-02-PLAN.md
@internal/tools/researcher/core/sources/refresh.ts
@internal/tools/researcher/core/reports/lineage.ts
@internal/tools/researcher/core/reports/backlinks.ts
@internal/tools/researcher/core/status/summary.ts
@internal/tools/researcher/__tests__/resume.spec.ts
@researcher/REPORTING.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Propagate stale/fresh changes from sources into dependent artifact health</name>
  <files>internal/tools/researcher/core/freshness/propagation.ts, internal/tools/researcher/core/sources/refresh.ts, internal/tools/researcher/core/status/summary.ts, internal/tools/researcher/__tests__/freshness-propagation.spec.ts</files>
  <behavior>
    - Test 1: marking a source stale marks dependent insights stale, marks affected reports stale, and surfaces impacted analyses in the derived summary.
    - Test 2: refreshing the source recomputes downstream health and clears stale flags only when no stale upstream support remains.
    - Test 3: overlapping lineage dedupes impacted IDs and does not require a second provenance index.
  </behavior>
  <action>Create a freshness propagation helper that resolves downstream insights, analyses, and reports from canonical lineage, then recomputes persisted/derived health after source refresh. Invoke it from the existing source refresh mutation boundary. Keep analyses read-derived, not persisted-stale. Do not create a parallel impact cache or source-first report-writing path.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/freshness-propagation.spec.ts` passes with stale, refreshed, and overlapping-lineage scenarios plus impacted-analysis coverage.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/freshness-propagation.spec.ts</automated>
  </verify>
  <done>Freshness impact flows deterministically through the graph and can be cleared without manual downstream edits.</done>
</task>

<task type="auto">
  <name>Task 2: Extend resume/status routing coverage for explicit downstream impact</name>
  <files>internal/tools/researcher/__tests__/resume.spec.ts, internal/tools/researcher/core/status/summary.ts</files>
  <behavior>
    - Test 1: stale freshness debt still produces `refresh-sources` even when reports and impacted downstream artifacts exist.
    - Test 2: once freshness clears, the next action can route to downstream review or regeneration instead of staying on refresh.
  </behavior>
  <action>Extend resume/status coverage so the richer Phase 5 impact model does not break the existing debt-first routing discipline. Keep this as coverage plus the minimum routing fix exposed by tests; do not redesign the progress-router model introduced earlier.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/resume.spec.ts` passes with explicit stale-priority and post-refresh routing assertions.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/resume.spec.ts</automated>
  </verify>
  <done>Routing remains stable and debt-aware after freshness propagation becomes explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source refresh -> downstream health state | Refresh operations must not leave stale downstream flags stuck or silently cleared incorrectly. |
| propagated impact -> operator action | Operators must see the real impacted layers without redundant or contradictory state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | T | `internal/tools/researcher/core/freshness/propagation.ts`, `core/sources/refresh.ts` | mitigate | Recompute downstream health from canonical lineage on every refresh transition and dedupe impacted IDs. |
| T-05-04 | R | `internal/tools/researcher/core/status/summary.ts`, `resume.spec.ts` | mitigate | Prove stale debt keeps priority over downstream review/regeneration once explicit impact tracking exists. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/freshness-propagation.spec.ts`
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/resume.spec.ts`
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `rg -n "stale|unsupported|impacted|refresh-sources" internal/tools/researcher/core/freshness/propagation.ts internal/tools/researcher/core/sources/refresh.ts internal/tools/researcher/core/status/summary.ts internal/tools/researcher/__tests__/freshness-propagation.spec.ts`
</verification>

<output>
After completion, create `.planning/phases/05-status-freshness-verification/05-03-SUMMARY.md`
</output>
