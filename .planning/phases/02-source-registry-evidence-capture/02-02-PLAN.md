---
phase: 02-source-registry-evidence-capture
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - internal/tools/researcher/core/sources/store.ts
  - internal/tools/researcher/core/sources/normalize.ts
  - internal/tools/researcher/core/sources/add.ts
  - internal/tools/researcher/__tests__/source-add.spec.ts
  - scripts/research-source-add.ts
autonomous: true
requirements:
  - SRC-01
  - SRC-02
user_setup: []
must_haves:
  truths:
    - "A user can add a source into one central structured registry and receive one stable `SRC-*` identity."
    - "Adding the same canonical locator twice updates one existing record instead of creating duplicate source IDs."
    - "Source metadata updates stay deterministic and provider-neutral because all JSON mutation goes through shared core tooling."
  artifacts:
    - "internal/tools/researcher/core/sources/add.ts provides the source add/upsert service over validated manifest and source documents."
    - "internal/tools/researcher/core/sources/normalize.ts owns canonical locator normalization used for dedupe."
    - "scripts/research-source-add.ts exposes a thin runtime-neutral CLI that prints deterministic JSON."
  key_links:
    - "internal/tools/researcher/core/sources/store.ts loads and atomically persists both `manifest.json` and `sources.json` through the existing validators and JSON writer."
    - "internal/tools/researcher/core/sources/add.ts allocates new IDs from `manifest.next_ids.source` only on create, not on duplicate updates."
    - "internal/tools/researcher/__tests__/source-add.spec.ts proves one canonical locator maps to one durable registry record."
---

<objective>
Implement the provider-neutral source add and metadata update flow on top of the Phase 2 contract.

Purpose: Deliver the first real Phase 2 mutation path so a research can accumulate durable source records without duplicate IDs or prompt-authored JSON edits.
Output: Shared source store helpers, canonical URL normalization, source add/upsert core, add-flow tests, and a thin `research-source-add` CLI.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-source-registry-evidence-capture/02-CONTEXT.md
@.planning/phases/02-source-registry-evidence-capture/02-RESEARCH.md
@.planning/phases/01-research-workspace-bootstrap/01-03-SUMMARY.md
@internal/tools/researcher/contracts/manifest.ts
@internal/tools/researcher/contracts/sources.ts
@internal/tools/researcher/contracts/validators.ts
@internal/tools/researcher/fs/workspace-paths.ts
@internal/tools/researcher/fs/write-json-atomically.ts
@scripts/research-init.ts
@scripts/research-resume.ts

<interfaces>
Existing allocator and manifest surface:

```typescript
export interface NextResearchIds {
  source: number;
  insight: number;
  analysis: number;
  report: number;
  task: number;
}

export function formatArtifactId(kind: "source", counter: number): string;
```

Thin CLI pattern to preserve:

```typescript
async function main(): Promise<void> {
  const args = parseCliArguments(process.argv.slice(2));
  const result = await sharedCore(args);
  process.stdout.write(`${JSON.stringify(result)}\n`);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement canonical source add/upsert and manifest synchronization</name>
  <files>internal/tools/researcher/core/sources/store.ts, internal/tools/researcher/core/sources/normalize.ts, internal/tools/researcher/core/sources/add.ts, internal/tools/researcher/__tests__/source-add.spec.ts</files>
  <read_first>.planning/phases/02-source-registry-evidence-capture/02-CONTEXT.md, internal/tools/researcher/contracts/manifest.ts, internal/tools/researcher/contracts/sources.ts, internal/tools/researcher/contracts/validators.ts, internal/tools/researcher/fs/workspace-paths.ts, internal/tools/researcher/fs/write-json-atomically.ts, internal/tools/researcher/__tests__/init.spec.ts, internal/tools/researcher/__tests__/resume.spec.ts</read_first>
  <behavior>
    - Test 1: adding a new source allocates the next `SRC-*` ID from `manifest.next_ids.source`, appends one record to `sources.json`, increments `manifest.inventory.sources`, and advances `manifest.next_ids.source` by exactly one.
    - Test 2: adding the same canonical locator again updates the existing record in place, returns an `updated` result, and leaves source count plus next-ID counter unchanged.
    - Test 3: invalid URLs, mismatched research identities, or duplicate requests that would bypass canonical normalization fail before any write.
  </behavior>
  <action>Create a shared source-store layer in `internal/tools/researcher/core/sources/store.ts` that loads `manifest.json` and `sources.json` through the existing validators, asserts `research.id === sources.research_id`, and persists both files atomically through `writeJsonAtomically`. In `normalize.ts`, implement canonical locator normalization per D-15 and D-16 using the WHATWG `URL` parser: trim whitespace, lower-case protocol and host, drop fragments, preserve query strings, and strip a trailing slash only when the pathname is not `/`. In `add.ts`, implement one provider-neutral add/upsert service per D-17, D-18, D-20, D-27, and D-29: accept structured source input, compute `canonical_url` from `canonicalUrl ?? url`, allocate `SRC-*` only on create, default new records to `status: queued`, `side_states: []`, `linked_insights: []`, `captures: []`, and `latest_capture_path: null`, update `updated_at`, `accessed_at`, and `last_checked_at` deterministically, and sync `manifest.inventory.sources`, `manifest.freshness.last_source_sync_at`, and `manifest.freshness.debt`. Do not fetch network content, write capture files, or introduce provider-specific search logic in this plan.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/source-add.spec.ts` passes with explicit create-vs-update assertions, and `rg -n "formatArtifactId\\(|canonical_url|inventory\\.sources|last_source_sync_at" internal/tools/researcher/core/sources/add.ts internal/tools/researcher/core/sources/normalize.ts internal/tools/researcher/core/sources/store.ts` shows allocator, dedupe, and manifest-sync logic in the shared core.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/source-add.spec.ts</automated>
  </verify>
  <done>Source creation and duplicate-safe metadata updates happen through one shared deterministic core service that keeps `manifest.json` and `sources.json` aligned.</done>
</task>

<task type="auto">
  <name>Task 2: Add the thin `research-source-add` CLI entrypoint</name>
  <files>scripts/research-source-add.ts, internal/tools/researcher/__tests__/source-add.spec.ts</files>
  <read_first>scripts/research-init.ts, scripts/research-resume.ts, internal/tools/researcher/core/sources/add.ts, internal/tools/researcher/__tests__/source-add.spec.ts</read_first>
  <action>Create `scripts/research-source-add.ts` as a thin argv parser per D-27 and D-28. It must accept `--project-root`, `--slug`, `--title`, `--url`, `--type`, `--origin-type`, `--origin-value`, and optional `--canonical-url`, `--confidence`, `--status`, repeated `--tag`, `--published-at`, `--accessed-at`, `--last-checked-at`, and `--notes`; then call the shared add core and print only deterministic JSON. Extend `source-add.spec.ts` with at least one CLI smoke test that runs the script against a temp workspace and asserts the stdout shape includes `operation`, `sourceId`, and `canonicalUrl`. Do not add slash-command wrappers, interactive prompts, or alternate output modes.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/source-add.spec.ts` passes with a CLI smoke assertion, and `rg -n "Unknown argument|Missing required argument|JSON\\.stringify" scripts/research-source-add.ts` shows the standard thin-CLI error and output pattern used by the existing Researcher entrypoints.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/source-add.spec.ts</automated>
  </verify>
  <done>The repo exposes a runtime-neutral add-source entrypoint that delegates all state mutation to the shared core and returns deterministic machine-readable results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/operator source metadata -> shared add core | Untrusted source input crosses into canonicalization, ID allocation, and durable registry mutation. |
| shared source store -> `manifest.json` + `sources.json` | Two authoritative machine-state files must stay synchronized after each add/upsert operation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | `internal/tools/researcher/core/sources/normalize.ts` | mitigate | Normalize one canonical locator per D-16 and use it for duplicate detection before any new `SRC-*` allocation. |
| T-02-05 | T | `internal/tools/researcher/core/sources/store.ts` | mitigate | Validate manifest and source documents on load, assert shared research identity, and persist both artifacts atomically through the shared JSON writer. |
| T-02-06 | D | `internal/tools/researcher/core/sources/add.ts` | mitigate | Reject invalid input early and keep the core provider-neutral so unexpected search/provider failures cannot corrupt the public registry. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/source-add.spec.ts`
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `rg -n "canonical_url|formatArtifactId|last_source_sync_at|operation" internal/tools/researcher/core/sources/add.ts internal/tools/researcher/core/sources/normalize.ts scripts/research-source-add.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- New sources allocate one stable `SRC-*` ID from the manifest-backed counter
- Duplicate canonical locators update one existing record instead of creating a second source entry
- Source metadata required by SRC-02 is persisted through the shared core, not by direct JSON edits
- The add CLI remains thin and prints deterministic JSON only
</success_criteria>

<output>
After completion, create `.planning/phases/02-source-registry-evidence-capture/02-02-SUMMARY.md`
</output>
