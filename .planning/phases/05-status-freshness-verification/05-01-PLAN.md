---
phase: 05-status-freshness-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - researcher/schemas/manifest.schema.json
  - researcher/schemas/insight-frontmatter.schema.json
  - researcher/schemas/report-frontmatter.schema.json
  - internal/tools/researcher/contracts/manifest.ts
  - internal/tools/researcher/contracts/insights.ts
  - internal/tools/researcher/contracts/reports.ts
  - internal/tools/researcher/contracts/validators.ts
  - internal/tools/researcher/core/artifacts/markdown.ts
  - internal/tools/researcher/contracts/status.ts
  - internal/tools/researcher/__tests__/status-contract.spec.ts
autonomous: true
requirements:
  - RSCH-02
  - STAT-02
user_setup: []
must_haves:
  truths:
    - "Manifest, insight, and report health metadata are frozen behind schema-validated contracts before any downstream propagation or status code lands."
    - "Insights and reports can persist Phase 5 health flags without conflating them with the editorial lifecycle status."
    - "One typed status-summary boundary exists for `/research-status`, rather than ad hoc JSON assembly spread across the repo."
  artifacts:
    - "Manifest, insight, and report schemas/types capture Phase 5 health metadata cleanly."
    - "internal/tools/researcher/contracts/status.ts defines the public status payload contract."
    - "internal/tools/researcher/__tests__/status-contract.spec.ts proves malformed health metadata is rejected."
  key_links:
    - "validators.ts compiles the extended schemas through the existing Ajv boundary."
    - "markdown.ts parses and renders any new frontmatter health fields through the shared artifact boundary."
    - "The status payload contract stays separate from manifest persistence so the manifest can remain compact."
---

<objective>
Freeze the Phase 5 health/state contract before any propagation or debt aggregation logic lands.

Purpose: Implement D-57 through D-64 at the contract layer so later status and propagation flows
reuse one inspectable schema-backed boundary.
Output: extended schemas/types, shared parse/render support, a typed status-summary contract, and
focused contract tests aligned with `05-VALIDATION.md`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-status-freshness-verification/05-CONTEXT.md
@.planning/phases/05-status-freshness-verification/05-RESEARCH.md
@.planning/phases/05-status-freshness-verification/05-VALIDATION.md
@.planning/phases/04-report-generation/04-VERIFICATION.md
@internal/tools/researcher/contracts/manifest.ts
@internal/tools/researcher/contracts/insights.ts
@internal/tools/researcher/contracts/reports.ts
@internal/tools/researcher/contracts/validators.ts
@internal/tools/researcher/core/artifacts/markdown.ts
@researcher/WORKFLOWS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend canonical artifact and manifest contracts for persisted health flags</name>
  <files>researcher/schemas/manifest.schema.json, researcher/schemas/insight-frontmatter.schema.json, researcher/schemas/report-frontmatter.schema.json, internal/tools/researcher/contracts/manifest.ts, internal/tools/researcher/contracts/insights.ts, internal/tools/researcher/contracts/reports.ts, internal/tools/researcher/__tests__/status-contract.spec.ts</files>
  <behavior>
    - Test 1: insights and reports accept valid `side_states` arrays while rejecting unknown health flags.
    - Test 2: manifest accepts compact verification summary counts but rejects malformed shapes or negative counts.
    - Test 3: the combined contract preserves existing editorial `status` semantics unchanged.
  </behavior>
  <action>Extend the manifest, insight, and report contracts to support Phase 5 persisted health metadata. Keep the manifest summary compact and aggregate-focused. Add `side_states` to insight and report frontmatter only; do not add a stale side-state contract to analyses. Reuse existing side-state naming patterns where sensible, but keep artifact health distinct from the editorial lifecycle field.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/status-contract.spec.ts` passes with explicit coverage for valid and invalid manifest health summaries plus valid and invalid insight/report side-state metadata.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/status-contract.spec.ts</automated>
  </verify>
  <done>Persisted Phase 5 health metadata is schema-validated and locked before status logic consumes it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a typed status-summary boundary and shared artifact parsing support</name>
  <files>internal/tools/researcher/contracts/status.ts, internal/tools/researcher/contracts/validators.ts, internal/tools/researcher/core/artifacts/markdown.ts, internal/tools/researcher/__tests__/status-contract.spec.ts</files>
  <behavior>
    - Test 1: the shared parser renders and parses any new artifact health fields deterministically.
    - Test 2: the public status-summary contract rejects missing impacted-ID sections or missing primary routing fields.
  </behavior>
  <action>Create a typed status-summary contract for `/research-status` and extend the shared artifact parser/render path to include any new persisted health metadata. Keep the status-summary contract read-oriented and separate from manifest persistence. Do not add an alternate parser path or a second summary format.</action>
  <acceptance_criteria>`rg -n "side_states|StatusSummary" internal/tools/researcher/contracts/status.ts internal/tools/researcher/core/artifacts/markdown.ts internal/tools/researcher/contracts/validators.ts` shows one consistent contract path, and `pnpm exec vitest run internal/tools/researcher/__tests__/status-contract.spec.ts` passes.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/status-contract.spec.ts</automated>
  </verify>
  <done>The repo has one shared contract boundary for persisted health metadata plus one typed public status payload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted health metadata -> status/debt evaluators | Malformed or partial health fields must not corrupt downstream routing or debt calculations. |
| compact manifest summary -> operator trust | Aggregate debt counts must not drift into verbose unbounded state that is hard to audit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | manifest / insight / report contracts | mitigate | Validate all new health metadata through Ajv and shared artifact parsing before any downstream status logic uses it. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/status-contract.spec.ts`
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `rg -n "side_states|verification|StatusSummary" researcher/schemas/manifest.schema.json researcher/schemas/insight-frontmatter.schema.json researcher/schemas/report-frontmatter.schema.json internal/tools/researcher/contracts/status.ts`
</verification>

<output>
After completion, create `.planning/phases/05-status-freshness-verification/05-01-SUMMARY.md`
</output>
