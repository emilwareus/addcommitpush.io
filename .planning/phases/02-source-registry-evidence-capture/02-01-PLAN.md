---
phase: 02-source-registry-evidence-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - researcher/schemas/sources.schema.json
  - internal/tools/researcher/contracts/sources.ts
  - internal/tools/researcher/contracts/validators.ts
  - internal/tools/researcher/contracts/workspace.ts
  - internal/tools/researcher/fs/workspace-paths.ts
  - internal/tools/researcher/__tests__/contracts.spec.ts
autonomous: true
requirements:
  - SRC-01
  - SRC-02
user_setup: []
must_haves:
  truths:
    - "The public `sources.json` ledger stays in place while Phase 2 source records gain stable IDs, dedupe keys, and explicit source metadata."
    - "Malformed URLs, malformed timestamps, and invalid capture refs are rejected before any Phase 2 writer trusts disk."
    - "Capture references are machine-readable root-relative paths under `data/`, never inline evidence blobs."
  artifacts:
    - "researcher/schemas/sources.schema.json defines the enriched Phase 2 source-record contract without changing the top-level `sources.json` artifact."
    - "internal/tools/researcher/contracts/sources.ts exports typed source status, origin, confidence, and capture-ref interfaces for later source writers."
    - "internal/tools/researcher/fs/workspace-paths.ts exposes root-confined helpers for validating `data/...` capture refs."
  key_links:
    - "internal/tools/researcher/contracts/validators.ts compiles the enriched schema with enforced URI/date-time validation."
    - "internal/tools/researcher/contracts/workspace.ts defines the `data/snapshots`, `data/exports`, `data/transcripts`, and `data/datasets` buckets used by later capture writers."
    - "internal/tools/researcher/__tests__/contracts.spec.ts proves the contract rejects invalid locators, timestamps, and escape-prone capture refs."
---

<objective>
Freeze the Phase 2 source-registry contract and filesystem guardrails before any add or refresh writer mutates a research workspace.

Purpose: Lock the enriched `sources.json` surface per the user’s decisions so later source tooling reuses one validated contract instead of inventing parallel shapes.
Output: Updated source schema/types, enforced format validation, `data/` bucket constants, capture-ref path guards, and active contract tests.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-source-registry-evidence-capture/02-CONTEXT.md
@.planning/phases/02-source-registry-evidence-capture/02-RESEARCH.md
@.planning/phases/01-research-workspace-bootstrap/01-02-SUMMARY.md
@researcher/CONTRACTS.md
@researcher/ARTIFACT-MODEL.md
@researcher/WORKFLOWS.md
@researcher/schemas/sources.schema.json
@internal/tools/researcher/contracts/sources.ts
@internal/tools/researcher/contracts/validators.ts
@internal/tools/researcher/contracts/workspace.ts
@internal/tools/researcher/fs/workspace-paths.ts

<interfaces>
Current public source envelope:

```typescript
export interface SourcesEnvelope {
  research_id: string;
  updated_at: string | null;
  sources: SourceRecord[];
}
```

Current manifest-backed ID allocator:

```typescript
export function formatArtifactId(kind: "source", counter: number): string;
// Example output: SRC-0001
```

Current bounded workspace root:

```typescript
export const RESEARCH_WORKSPACE_ROOT = "researcher/researches";
export function resolveWorkspacePath(
  projectRoot: string,
  slug: string,
  workspaceRelativePath: string,
): Promise<string>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the enriched Phase 2 source record and `data/` bucket contract</name>
  <files>researcher/schemas/sources.schema.json, internal/tools/researcher/contracts/sources.ts, internal/tools/researcher/contracts/workspace.ts</files>
  <read_first>.planning/phases/02-source-registry-evidence-capture/02-CONTEXT.md, .planning/phases/02-source-registry-evidence-capture/02-RESEARCH.md, researcher/CONTRACTS.md, researcher/ARTIFACT-MODEL.md, researcher/schemas/sources.schema.json, internal/tools/researcher/contracts/sources.ts, internal/tools/researcher/contracts/workspace.ts</read_first>
  <action>Per D-14 through D-23, keep `sources.json` as the public registry artifact and replace the current minimal `SourceRecord` contract with a typed Phase 2 shape that includes one stable `SRC-*` ID, `canonical_url`, `origin`, `type`, `confidence`, one primary workflow `status`, separate `side_states`, `published_at`, `created_at`, `updated_at`, `accessed_at`, `last_checked_at`, `latest_capture_path`, append-only `captures`, `linked_insights`, `tags`, and `notes`. Rename the old requirement-mismatched `credibility` field to `confidence`, remove the singular `snapshot_path`, and keep capture refs as root-relative strings only. In `researcher/schemas/sources.schema.json`, require `captures[*].path` and `latest_capture_path` to live under `data/`, require `linked_insights` to stay `INS-*`, and keep the top-level envelope shape unchanged. In `internal/tools/researcher/contracts/workspace.ts`, export explicit bucket constants and helper builders for `data/snapshots`, `data/exports`, `data/transcripts`, and `data/datasets` so later capture writers reuse one path contract. Do not add provider-specific fields, alternate ledger files, or downstream Phase 5 freshness propagation fields.</action>
  <acceptance_criteria>`rg -n "canonical_url|side_states|confidence|latest_capture_path|captures|DATA_(SNAPSHOTS|EXPORTS|TRANSCRIPTS|DATASETS)" researcher/schemas/sources.schema.json internal/tools/researcher/contracts/sources.ts internal/tools/researcher/contracts/workspace.ts` returns the new contract fields and bucket constants, and `rg -n "credibility|snapshot_path" researcher/schemas/sources.schema.json internal/tools/researcher/contracts/sources.ts` returns no matches.</acceptance_criteria>
  <verify>
    <automated>rg -n "canonical_url|side_states|confidence|latest_capture_path|captures|DATA_(SNAPSHOTS|EXPORTS|TRANSCRIPTS|DATASETS)" researcher/schemas/sources.schema.json internal/tools/researcher/contracts/sources.ts internal/tools/researcher/contracts/workspace.ts</automated>
  </verify>
  <done>The Phase 2 source contract fully expresses dedupe, operator workflow state, source metadata, and root-relative evidence references without changing the public `sources.json` artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce source formats and root-relative capture-ref validation</name>
  <files>package.json, pnpm-lock.yaml, internal/tools/researcher/contracts/validators.ts, internal/tools/researcher/fs/workspace-paths.ts, internal/tools/researcher/__tests__/contracts.spec.ts</files>
  <read_first>researcher/schemas/sources.schema.json, internal/tools/researcher/contracts/sources.ts, internal/tools/researcher/contracts/validators.ts, internal/tools/researcher/fs/workspace-paths.ts, internal/tools/researcher/__tests__/contracts.spec.ts, package.json</read_first>
  <behavior>
    - Test 1: `validateSourcesEnvelope()` rejects malformed `url`, `canonical_url`, and date-time fields instead of accepting them as documentation-only schema hints.
    - Test 2: capture refs like `/tmp/file.html`, `../outside.txt`, or `reports/file.md` fail before any writer can persist them.
    - Test 3: a valid source record with `data/snapshots/SRC-0001/...` capture refs passes the shared validator boundary.
  </behavior>
  <action>Install `ajv-formats` and wire it into `internal/tools/researcher/contracts/validators.ts` so URI and `date-time` checks are actually enforced, per D-18 and the Phase 2 research guidance. Extend `internal/tools/researcher/fs/workspace-paths.ts` with one shared helper that validates root-relative capture refs under `data/` only, rejects absolute paths, traversal, and non-`data/` folders per D-19, D-21, and D-22, and is reusable by later capture writers. Replace the current light contract spec with active tests covering invalid locator/timestamp rejection, valid Phase 2 source-record acceptance, and capture-ref escape rejection. Keep validation deterministic and fail-fast; do not add fallback parsing or alternate validation paths.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/contracts.spec.ts` passes with explicit assertions for invalid URI/date-time payloads and invalid capture refs, `package.json` includes `ajv-formats`, and `rg -n "validateFormats: false" internal/tools/researcher/contracts/validators.ts` returns no matches.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/contracts.spec.ts</automated>
  </verify>
  <done>The Phase 2 source contract is enforced at the shared validator boundary, and escape-prone capture refs are rejected before any writer can drift evidence outside `data/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/provider metadata -> `sources.json` validator | Untrusted URLs, timestamps, and status values cross into durable machine state. |
| persisted capture refs -> workspace path resolver | Root-relative evidence references cross into filesystem path construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `internal/tools/researcher/contracts/validators.ts` | mitigate | Enforce URI and `date-time` formats with `ajv-formats`, then prove invalid payloads fail in `contracts.spec.ts`. |
| T-02-02 | T | `internal/tools/researcher/fs/workspace-paths.ts` | mitigate | Add one shared capture-ref validator that only accepts root-relative `data/...` refs and rejects absolute paths, traversal, and non-`data/` folders. |
| T-02-03 | R | `researcher/schemas/sources.schema.json` | mitigate | Preserve one inspectable public schema and one typed source contract so later add/refresh writers cannot silently drift from the locked Phase 2 ledger shape. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm install`
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/contracts.spec.ts`
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `rg -n "canonical_url|side_states|latest_capture_path|captures" researcher/schemas/sources.schema.json internal/tools/researcher/contracts/sources.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The only public source ledger remains `sources.json`
- The Phase 2 source contract includes dedupe keys, required metadata, root-relative capture refs, and downstream linkage placeholders
- Invalid URLs, timestamps, and non-`data/` capture refs fail at the shared validator boundary
- `workspace.ts` exposes reusable `data/` bucket constants for later source capture writers
</success_criteria>

<output>
After completion, create `.planning/phases/02-source-registry-evidence-capture/02-01-SUMMARY.md`
</output>
