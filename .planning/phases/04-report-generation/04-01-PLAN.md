---
phase: 04-report-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - researcher/templates/REPORT.md
  - researcher/schemas/report-frontmatter.schema.json
  - internal/tools/researcher/contracts/reports.ts
  - internal/tools/researcher/contracts/validators.ts
  - internal/tools/researcher/core/artifacts/markdown.ts
  - internal/tools/researcher/__tests__/report-contract.spec.ts
autonomous: true
requirements:
  - RPT-01
  - RPT-03
user_setup: []
must_haves:
  truths:
    - "Canonical `RPT-*` artifacts have schema-validated frontmatter, fixed ordered sections, and explicit lineage fields before any report writer touches disk."
    - "Report artifacts remain Markdown-first and reuse the shared artifact boundary instead of introducing a report-only parser or ad hoc template handling."
    - "Malformed analysis, insight, or source reference sections are rejected before later report upsert flows can persist unsupported packaging."
  artifacts:
    - "researcher/schemas/report-frontmatter.schema.json and internal/tools/researcher/contracts/reports.ts freeze the report contract."
    - "internal/tools/researcher/core/artifacts/markdown.ts parses and renders report artifacts through the same shared boundary already used by `INS-*` and `ANL-*`."
    - "internal/tools/researcher/__tests__/report-contract.spec.ts proves report contract validity, invalid frontmatter failure, section-order enforcement, and reference-section parsing."
  key_links:
    - "internal/tools/researcher/contracts/validators.ts compiles the new report schema through the existing Ajv boundary."
    - "internal/tools/researcher/core/artifacts/markdown.ts adds `parseReportArtifact()` and `renderReportArtifact()` without forking parser logic."
    - "researcher/templates/REPORT.md aligns with the locked Phase 4 frontmatter and body-section decisions."
---

<objective>
Freeze the canonical report artifact contract before any report packaging or lineage-sync code lands.

Purpose: Implement D-43 through D-50 and D-54 at the contract layer so later report generation
flows can reuse one inspectable schema-backed boundary.
Output: report template, frontmatter schema, typed report contracts, shared parser/render support,
and contract tests aligned with `04-VALIDATION.md`.
</objective>

<execution_note>
This plan intentionally keeps the report contract and shared parser extension together so later
waves inherit one stable artifact boundary. Execute Task 1 before Task 2 and do not parallelize
them inside this plan.
</execution_note>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-report-generation/04-CONTEXT.md
@.planning/phases/04-report-generation/04-RESEARCH.md
@.planning/phases/04-report-generation/04-VALIDATION.md
@.planning/phases/03-insights-analysis-graph/03-01-SUMMARY.md
@.planning/phases/03-insights-analysis-graph/03-03-SUMMARY.md
@researcher/REPORTING.md
@researcher/templates/REPORT.md
@researcher/examples/report-request.xml
@internal/tools/researcher/contracts/analysis.ts
@internal/tools/researcher/contracts/insights.ts
@internal/tools/researcher/contracts/validators.ts
@internal/tools/researcher/core/artifacts/markdown.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the canonical report frontmatter and body contract</name>
  <files>researcher/templates/REPORT.md, researcher/schemas/report-frontmatter.schema.json, internal/tools/researcher/contracts/reports.ts, internal/tools/researcher/__tests__/report-contract.spec.ts</files>
  <behavior>
    - Test 1: a valid report document with `RPT-*` frontmatter, explicit audience/angle/thesis, and fixed `Summary -> Key Points -> Body -> Limitations -> Analysis Inputs -> Insight References -> Source References` sections parses successfully.
    - Test 2: malformed report IDs, missing `fresh_as_of`, missing reference sections, or out-of-order headings are rejected.
    - Test 3: report frontmatter allows both `derived_from_analysis` and `derived_from_insights`, but rejects empty combined lineage.
  </behavior>
  <action>Create a report frontmatter schema and typed contract that include `id`, `title`, `audience`, `angle`, `thesis`, `status`, `derived_from_analysis`, `derived_from_insights`, `fresh_as_of`, `created_at`, and `updated_at`. Reuse the shared status vocabulary already established for other artifact kinds. Keep report lineage rooted in `ANL-*` and `INS-*` IDs only; do not place raw `SRC-*` IDs in frontmatter. Update `researcher/templates/REPORT.md` so the fixed body sections match the locked Phase 4 decisions and remain packaging-oriented rather than notebook-like or source-note-like.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/report-contract.spec.ts` passes with explicit cases for valid reports, invalid IDs/timestamps, missing sections, malformed lineage arrays, and heading-order drift.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/report-contract.spec.ts</automated>
  </verify>
  <done>Canonical `RPT-*` Markdown artifacts are fully specified in schema, template, and typed contracts before report writes begin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the shared Markdown parser/render boundary for report artifacts</name>
  <files>internal/tools/researcher/contracts/validators.ts, internal/tools/researcher/core/artifacts/markdown.ts, internal/tools/researcher/__tests__/report-contract.spec.ts</files>
  <behavior>
    - Test 1: the shared parser validates report frontmatter through Ajv and returns typed section payloads.
    - Test 2: report reference sections parse into machine-readable analysis, insight, and source references with required IDs and support text.
    - Test 3: canonical rendering preserves ordered sections and deterministic array serialization.
  </behavior>
  <action>Extend the shared validator boundary and artifact parser with `parseReportArtifact()` and `renderReportArtifact()`. Keep report sections deterministic and tool-owned. Parse `Analysis Inputs`, `Insight References`, and `Source References` as structured bullet lists suitable for later lineage rendering. Do not add alternate parsing paths, freeform heading tolerance, or a separate report-specific Markdown engine.</action>
  <acceptance_criteria>`rg -n "parseReportArtifact|renderReportArtifact|validateReportFrontmatter" internal/tools/researcher/core/artifacts/markdown.ts internal/tools/researcher/contracts/validators.ts` shows one shared path, and `pnpm exec vitest run internal/tools/researcher/__tests__/report-contract.spec.ts` passes.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/report-contract.spec.ts</automated>
  </verify>
  <done>The repo has one shared deterministic parse/render boundary for all artifact kinds, including reports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-authored report request -> canonical report artifact | Untrusted audience/angle/thesis text must not bypass schema validation or ordered section requirements. |
| rendered reference sections -> downstream verification/report consumption | Report lineage must remain explicit and parseable so later freshness and verification phases can trust it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `internal/tools/researcher/core/artifacts/markdown.ts` | mitigate | Validate report frontmatter through Ajv, enforce exact section order, and reject malformed lineage-reference bullets. |
| T-04-01A | R | `researcher/templates/REPORT.md`, `researcher/schemas/report-frontmatter.schema.json` | mitigate | Freeze one inspectable report template and schema pair before later packaging code writes reports. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/report-contract.spec.ts`
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `rg -n "derived_from_analysis|derived_from_insights|fresh_as_of|Analysis Inputs|Insight References|Source References" researcher/templates/REPORT.md researcher/schemas/report-frontmatter.schema.json internal/tools/researcher/core/artifacts/markdown.ts`
</verification>

<output>
After completion, create `.planning/phases/04-report-generation/04-01-SUMMARY.md`
</output>
