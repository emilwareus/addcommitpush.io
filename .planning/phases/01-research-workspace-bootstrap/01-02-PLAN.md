---
phase: 01-research-workspace-bootstrap
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - researcher/schemas/manifest.schema.json
  - internal/tools/researcher/contracts/manifest.ts
  - internal/tools/researcher/contracts/sources.ts
  - internal/tools/researcher/contracts/workspace.ts
  - internal/tools/researcher/contracts/validators.ts
  - internal/tools/researcher/fs/workspace-paths.ts
  - internal/tools/researcher/__tests__/contracts.spec.ts
autonomous: true
requirements:
  - RSCH-01
user_setup: []
must_haves:
  truths:
    - "The bounded Phase 1 workspace contract is explicit, versioned, and validated in deterministic tooling."
    - "Structured workspace path resolution is confined to `researcher/researches/<slug>/` and rejects traversal or root confusion."
    - "Later init and resume flows share one manifest contract and one public `sources.json` envelope instead of redefining them."
  artifacts:
    - "researcher/schemas/manifest.schema.json defines the machine-readable manifest contract for Phase 1."
    - "internal/tools/researcher/contracts/validators.ts validates `manifest.json` and `sources.json` through shared tooling."
    - "internal/tools/researcher/fs/workspace-paths.ts resolves slugs into `researcher/researches/<slug>/` for every later writer and reader."
  key_links:
    - "internal/tools/researcher/contracts/workspace.ts centralizes the fixed public root per D-02 and D-03."
    - "internal/tools/researcher/contracts/validators.ts uses Ajv against the schema and source envelope before later flows trust disk."
    - "internal/tools/researcher/__tests__/contracts.spec.ts turns the Wave 0 scaffold into active contract and traversal tests."
---

<objective>
Freeze the Phase 1 workspace contract, schema validators, and root-confined path resolution on top of the Wave 0 test harness.

Purpose: Lock the public `researcher/researches/<slug>/` surface early so init and resume cannot drift from the user's decisions.
Output: Contract modules, manifest schema, shared validators, root guard logic, and active contract tests.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-research-workspace-bootstrap/01-CONTEXT.md
@.planning/phases/01-research-workspace-bootstrap/01-RESEARCH.md
@.planning/phases/01-research-workspace-bootstrap/01-VALIDATION.md
@researcher/README.md
@researcher/DESIGN.md
@researcher/ARTIFACT-MODEL.md
@researcher/WORKFLOWS.md
@researcher/CONTRACTS.md
@researcher/schemas/sources.schema.json
@internal/tools/researcher/__tests__/contracts.spec.ts

<interfaces>
Public workspace contract to preserve exactly:

```text
researcher/researches/<slug>/
├── brief.md
├── manifest.json
├── sources.json
├── insights/
├── data/
├── analysis/
└── reports/
```

Public `sources.json` envelope to preserve:

```json
{
  "research_id": "RES-20260410-deep-research-os",
  "updated_at": "2026-04-10T00:00:00Z",
  "sources": []
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the Phase 1 manifest schema and fixed workspace contract</name>
  <files>researcher/schemas/manifest.schema.json, internal/tools/researcher/contracts/manifest.ts, internal/tools/researcher/contracts/sources.ts, internal/tools/researcher/contracts/workspace.ts</files>
  <read_first>.planning/phases/01-research-workspace-bootstrap/01-CONTEXT.md, .planning/phases/01-research-workspace-bootstrap/01-RESEARCH.md, researcher/ARTIFACT-MODEL.md, researcher/CONTRACTS.md, researcher/DESIGN.md, researcher/schemas/sources.schema.json</read_first>
  <action>Create the Phase 1 manifest contract and workspace constants per D-01 through D-10. `manifest.schema.json` must include `contract_version`, `research.id`, `research.slug`, `research.title`, `status.state`, `status.stage`, `questions.active`, `freshness.window_days`, `freshness.last_source_sync_at`, `freshness.debt`, `inventory.sources`, `inventory.insights`, `inventory.analysis`, `inventory.reports`, and `paths.*` for the fixed root-relative artifact locations. `workspace.ts` must hard-code the public workspace root as `researcher/researches/<slug>/` per D-02 and D-03. Do not add Phase 2 source-capture fields, runtime-install logic, or user-defined layouts.</action>
  <verify>
    <automated>rg -n "contract_version|freshness|inventory|researcher/researches" researcher/schemas/manifest.schema.json internal/tools/researcher/contracts/manifest.ts internal/tools/researcher/contracts/workspace.ts</automated>
  </verify>
  <done>The fixed Phase 1 workspace contract and manifest surface are explicit and versioned in shared contract files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared validators, path guards, and active contract tests</name>
  <files>internal/tools/researcher/contracts/validators.ts, internal/tools/researcher/fs/workspace-paths.ts, internal/tools/researcher/__tests__/contracts.spec.ts</files>
  <read_first>researcher/schemas/manifest.schema.json, internal/tools/researcher/contracts/manifest.ts, internal/tools/researcher/contracts/sources.ts, internal/tools/researcher/contracts/workspace.ts, internal/tools/researcher/__tests__/contracts.spec.ts</read_first>
  <behavior>
    - Test 1: valid `manifest.json` and public `sources.json` payloads pass shared validation.
    - Test 2: absolute paths, `..`, and root-escape attempts are rejected before any filesystem access.
    - Test 3: the resolved workspace root is always `researcher/researches/<slug>/` for safe slugs.
  </behavior>
  <action>Create `validators.ts` and `workspace-paths.ts` to enforce the contract from Task 1. `validators.ts` must use `Ajv` to validate both `manifest.json` and the public `sources.json` envelope while keeping the later append-friendly ledger boundary behind shared tooling per D-05 through D-07. `workspace-paths.ts` must reject `..`, absolute paths, and symlink or root-escape attempts before any file read or write. Replace the Wave 0 placeholders in `contracts.spec.ts` with real assertions for schema validation and traversal rejection.</action>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/contracts.spec.ts</automated>
  </verify>
  <done>The Phase 1 contract is schema-backed and guarded against reads or writes outside the bounded research root.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user slug/input -> workspace path resolver | Untrusted input crosses into filesystem path construction. |
| contract helpers -> on-disk JSON artifacts | Structured machine state is persisted and later trusted by init and resume flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | `internal/tools/researcher/fs/workspace-paths.ts` | mitigate | Reject absolute paths, `..`, and resolved paths outside `researcher/researches/<slug>/`; prove it in active contract tests. |
| T-01-05 | T | `internal/tools/researcher/contracts/validators.ts` | mitigate | Validate manifest and source-envelope files at every read/write boundary with Ajv and fail fast on invalid data. |
| T-01-06 | I | `internal/tools/researcher/contracts/workspace.ts` | accept | Phase 1 paths are project-local and contain no secrets once root confinement is enforced. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint`
- [ ] `pnpm typecheck`
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/contracts.spec.ts`
- [ ] `rg -n "researcher/researches|contract_version|Ajv" internal/tools/researcher researcher/schemas/manifest.schema.json`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The only public Phase 1 workspace root is `researcher/researches/<slug>/`
- Manifest and source-envelope validation are centralized in shared deterministic tooling
- Contract tests prove traversal rejection and schema validation through the existing Wave 0 spec file
</success_criteria>

<output>
After completion, create `.planning/phases/01-research-workspace-bootstrap/01-02-SUMMARY.md`
</output>
