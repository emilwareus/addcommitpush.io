---
phase: 06-runtime-installation-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - researcher/schemas/runtime-install.schema.json
  - internal/tools/researcher/contracts/runtime.ts
  - internal/tools/researcher/contracts/validators.ts
  - internal/tools/researcher/runtime/catalog.ts
  - internal/tools/researcher/runtime/payload.ts
  - internal/tools/researcher/runtime/manifest.ts
  - internal/tools/researcher/__tests__/runtime-contract.spec.ts
autonomous: true
requirements:
  - INST-01
  - INST-02
  - INST-03
  - INST-04
user_setup: []
must_haves:
  truths:
    - "Phase 6 starts from one runtime-neutral command and asset catalog rather than hand-maintained Claude and Codex trees."
    - "Every installed runtime command resolves to an explicit wrapper script and shared payload dependency set, including runtime-loaded schemas, not an implicit repo-relative assumption."
    - "Install, update, and inspect all consume one schema-validated install-manifest contract with explicit ownership metadata."
    - "Runtime contracts stay detached from concrete target directories so later runtimes can reuse the same catalog without redesign."
  artifacts:
    - "researcher/schemas/runtime-install.schema.json and internal/tools/researcher/contracts/runtime.ts freeze the install request, asset record, and install-manifest contracts."
    - "internal/tools/researcher/runtime/catalog.ts and payload.ts define the shared Researcher command inventory plus the concrete wrapper/core/schema/template payload each command needs."
    - "internal/tools/researcher/__tests__/runtime-contract.spec.ts proves invalid runtimes, malformed manifests, and incomplete ownership data are rejected."
  key_links:
    - "internal/tools/researcher/contracts/validators.ts compiles the runtime schema through the existing Ajv boundary."
    - "internal/tools/researcher/runtime/payload.ts freezes the command-to-wrapper and wrapper-to-payload mapping against the current repo-owned scripts and templates."
    - "internal/tools/researcher/runtime/manifest.ts exposes one deterministic read/write shape for install ownership data."
    - "No adapter or lifecycle service bypasses the shared contract layer."
---

<objective>
Freeze the runtime-neutral install contract and ownership model before any adapter rendering or
lifecycle mutation lands.

Purpose: Implement D-72 through D-75 and D-79 through D-81 at the contract layer so adapter and
installer code inherit one inspectable source of truth.
Output: runtime schema, typed contracts, shared command and payload catalogs, install-manifest
helpers, and contract tests aligned with `06-VALIDATION.md`.
</objective>

<execution_note>
This plan intentionally locks the command catalog and ownership model before any runtime-specific
adapter code lands. Execute Task 1 before Task 2 and do not parallelize them inside this plan.
</execution_note>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-runtime-installation-lifecycle/06-CONTEXT.md
@.planning/phases/06-runtime-installation-lifecycle/06-RESEARCH.md
@.planning/phases/06-runtime-installation-lifecycle/06-VALIDATION.md
@.planning/phases/05-status-freshness-verification/05-VERIFICATION.md
@researcher/README.md
@researcher/WORKFLOWS.md
@researcher/CONTRACTS.md
@internal/tools/researcher/contracts/validators.ts
@go-research/external_code/get-shit-done/bin/install.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the runtime install and ownership contracts</name>
  <files>researcher/schemas/runtime-install.schema.json, internal/tools/researcher/contracts/runtime.ts, internal/tools/researcher/contracts/validators.ts, internal/tools/researcher/__tests__/runtime-contract.spec.ts</files>
  <behavior>
    - Test 1: valid install requests and manifests for `codex` and `claude` parse successfully.
    - Test 2: unsupported runtimes, missing managed-asset ownership fields, malformed relative paths, and incomplete version/source metadata are rejected.
    - Test 3: command records with missing wrapper targets or payload references are rejected.
    - Test 4: optional settings and hook intents remain explicit fields rather than hidden booleans or ad hoc comments.
  </behavior>
  <action>Create a runtime install schema and typed contracts for install requests, managed asset records, command records, payload records, settings merge records, and persisted install manifests. Require explicit runtime, asset kind, relative path, ownership metadata, version/source identity, and command-to-wrapper mapping for every managed file family. The payload contract must explicitly support non-code runtime assets such as `researcher/schemas/*.json` in addition to scripts, templates, and core modules. Keep the install-manifest shape runtime-neutral and suitable for install, update, and inspect. Do not add runtime-specific special cases to the public contract.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/runtime-contract.spec.ts` passes with explicit coverage for valid Codex and Claude install manifests plus invalid runtime, path, ownership, and command-mapping cases.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/runtime-contract.spec.ts</automated>
  </verify>
  <done>The repo has one schema-backed contract for runtime installation ownership and lifecycle state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the shared runtime command catalog and payload map</name>
  <files>internal/tools/researcher/runtime/catalog.ts, internal/tools/researcher/runtime/payload.ts, internal/tools/researcher/runtime/manifest.ts, internal/tools/researcher/__tests__/runtime-contract.spec.ts</files>
  <behavior>
    - Test 1: the shared catalog exposes the full Researcher command inventory exactly once, independent of runtime.
    - Test 2: every catalog entry resolves to a concrete wrapper script and the required shared payload files, including runtime-loaded schemas, without adapter-specific branching.
    - Test 3: the catalog freezes explicit mapping from conceptual runtime verbs to the currently implemented wrapper set.
    - Test 4: manifest helpers round-trip install records deterministically.
  </behavior>
  <action>Create one runtime-neutral command catalog plus a shared payload map that defines the Researcher lifecycle surfaces, descriptions, concrete wrapper targets, and required core/schema/template files. Freeze the command mapping against the currently implemented wrapper inventory in this repo so installed runtime entries resolve to real executable assets. Pair the catalog and payload map with manifest helpers that persist the ownership records from Task 1 without inventing a second storage shape. Do not hand-author separate Claude and Codex command inventories in this plan.</action>
  <acceptance_criteria>`rg -n "runtimeCatalog|payloadCatalog|research-init|research-source-add|research-source-refresh|research-insight|research-analysis|research-report|research-status|research-resume" internal/tools/researcher/runtime/catalog.ts internal/tools/researcher/runtime/payload.ts internal/tools/researcher/contracts/runtime.ts` shows one shared catalog-to-wrapper path, and `pnpm exec vitest run internal/tools/researcher/__tests__/runtime-contract.spec.ts` passes.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/runtime-contract.spec.ts</automated>
  </verify>
  <done>Adapter and lifecycle work can now consume one shared runtime catalog and one install-manifest boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-supplied install target -> managed runtime assets | Invalid runtime requests or malformed relative paths must not write outside the target project or create ambiguous ownership state. |
| persisted install manifest -> update and inspect | Incomplete or drifted manifest records must not cause silent overwrite of unmanaged files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | runtime contracts and install manifest | mitigate | Validate all install requests and persisted ownership data through Ajv before adapters or lifecycle services consume them. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/runtime-contract.spec.ts`
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `rg -n "InstallManifest|runtimeCatalog|payloadCatalog|research-init|research-source-add|research-source-refresh|research-insight|research-analysis|research-report|research-status|research-resume" researcher/schemas/runtime-install.schema.json internal/tools/researcher/contracts/runtime.ts internal/tools/researcher/runtime/catalog.ts internal/tools/researcher/runtime/payload.ts internal/tools/researcher/runtime/manifest.ts`
</verification>

<output>
After completion, create `.planning/phases/06-runtime-installation-lifecycle/06-01-SUMMARY.md`
</output>
