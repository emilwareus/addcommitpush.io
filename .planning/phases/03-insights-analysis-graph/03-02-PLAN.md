---
phase: 03-insights-analysis-graph
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - internal/tools/researcher/core/insights/backlinks.ts
  - internal/tools/researcher/core/insights/upsert.ts
  - internal/tools/researcher/__tests__/insight-upsert.spec.ts
  - internal/tools/researcher/__tests__/insight-lineage.spec.ts
  - scripts/research-insight.ts
autonomous: true
requirements:
  - INS-01
  - INS-02
user_setup: []
must_haves:
  truths:
    - "A user can promote gathered material into one canonical `INS-*` Markdown artifact with a stable manifest-backed ID."
    - "Every insight links to one or more supporting `SRC-*` records in both the artifact body and `sources.json[*].linked_insights`."
    - "Obvious duplicate insights are rejected deterministically before the manifest counter advances or a second insight file appears."
  artifacts:
    - "internal/tools/researcher/core/insights/upsert.ts provides deterministic create/update behavior for canonical insight artifacts."
    - "internal/tools/researcher/core/insights/backlinks.ts reconciles `sources.json[*].linked_insights` from the insight’s forward lineage."
    - "scripts/research-insight.ts exposes a thin JSON-only runtime entrypoint over the shared insight core."
  key_links:
    - "internal/tools/researcher/core/insights/upsert.ts reuses the existing Phase 2 source store and manifest counters instead of inventing a second state store."
    - "internal/tools/researcher/core/insights/backlinks.ts diffs old vs. new `derived_from_sources` sets so dropped source links are removed in the same write."
    - "internal/tools/researcher/__tests__/insight-upsert.spec.ts and internal/tools/researcher/__tests__/insight-lineage.spec.ts prove ID allocation, duplicate rejection, backlink add/remove behavior, and missing-source failure."
---

<objective>
Build canonical insight promotion and source-to-insight lineage on top of the Phase 3 artifact contracts.

Purpose: Deliver INS-01 and INS-02 by turning sourced evidence into reusable `INS-*` Markdown artifacts while keeping `sources.json` as the canonical source-lineage entrypoint per D-34 through D-36 and D-42.
Output: Shared insight upsert and backlink-sync helpers, lineage regression tests, and a thin `research-insight` CLI.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-insights-analysis-graph/03-CONTEXT.md
@.planning/phases/03-insights-analysis-graph/03-RESEARCH.md
@.planning/phases/03-insights-analysis-graph/03-VALIDATION.md
@.planning/phases/02-source-registry-evidence-capture/02-02-SUMMARY.md
@.planning/phases/02-source-registry-evidence-capture/02-03-SUMMARY.md
@internal/tools/researcher/contracts/manifest.ts
@internal/tools/researcher/contracts/sources.ts
@internal/tools/researcher/core/sources/store.ts
@internal/tools/researcher/fs/workspace-paths.ts
@scripts/research-source-add.ts

<interfaces>
Existing Phase 2 store to reuse:

```typescript
export interface SourceStoreState {
  workspaceRoot: string;
  manifestPath: string;
  sourcesPath: string;
  manifest: ResearchManifest;
  sources: SourcesEnvelope;
}

export async function loadSourceStore(input: {
  projectRoot: string;
  slug: string;
}): Promise<SourceStoreState>;

export async function persistSourceStore(state: SourceStoreState): Promise<void>;
```

Planned outputs from `03-01` to build against:

```typescript
export interface InsightFrontmatter {
  id: string;
  title: string;
  status: "draft" | "validated" | "disputed" | "superseded";
  confidence: "high" | "medium" | "low" | "unknown";
  derived_from_sources: string[];
  tags: string[];
  linked_analysis: string[];
  linked_reports: string[];
  created_at: string;
  updated_at: string;
}

export interface ParsedInsightDocument {
  frontmatter: InsightFrontmatter;
  sections: {
    claim: string;
    whyItMatters: string;
    evidence: Array<{ sourceId: string; note: string }>;
    caveats: string[];
    reuseNotes: string;
  };
}

export function parseInsightArtifact(markdown: string): ParsedInsightDocument;
export function renderInsightArtifact(document: ParsedInsightDocument): string;
export function createInsightFingerprint(document: ParsedInsightDocument): {
  titleFingerprint: string;
  claimFingerprint: string;
  sourceFingerprint: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement insight upsert, duplicate rejection, and source backlink reconciliation</name>
  <files>internal/tools/researcher/core/insights/backlinks.ts, internal/tools/researcher/core/insights/upsert.ts, internal/tools/researcher/__tests__/insight-upsert.spec.ts, internal/tools/researcher/__tests__/insight-lineage.spec.ts</files>
  <read_first>.planning/phases/03-insights-analysis-graph/03-CONTEXT.md, .planning/phases/03-insights-analysis-graph/03-RESEARCH.md, .planning/phases/03-insights-analysis-graph/03-VALIDATION.md, internal/tools/researcher/contracts/manifest.ts, internal/tools/researcher/contracts/sources.ts, internal/tools/researcher/core/sources/store.ts, internal/tools/researcher/fs/workspace-paths.ts, internal/tools/researcher/__tests__/source-add.spec.ts, internal/tools/researcher/__tests__/source-refresh.spec.ts</read_first>
  <behavior>
    - Test 1: creating a new insight allocates the next `INS-*` from `manifest.next_ids.insight`, writes `insights/<INS-ID>-<slug>.md`, increments `manifest.inventory.insights`, and appends the insight ID to each referenced source’s `linked_insights` array.
    - Test 2: updating an existing insight preserves its stable ID, rewrites the canonical file if the title slug changes, and removes backlinks from sources no longer listed in `derived_from_sources`.
    - Test 3: missing `SRC-*` references or obvious duplicates are rejected before any file write or counter increment, using D-36’s normalized title/claim fingerprints plus overlapping source lineage.
  </behavior>
  <action>Per D-30 through D-36 and D-42, create `internal/tools/researcher/core/insights/upsert.ts` and `internal/tools/researcher/core/insights/backlinks.ts` on top of the shared parser from `03-01` plus the existing Phase 2 source store. Use `loadSourceStore()` / `persistSourceStore()` for manifest and `sources.json` mutation so this plan reuses the existing deterministic state boundary instead of inventing a parallel store. Allocate IDs from `manifest.next_ids.insight`, write canonical filenames as `<INS-ID>-<slug>.md` where the ID remains authoritative per D-30, and rename the file on update if the human-readable title slug changes. Validate that every `derived_from_sources` entry exists in `sources.json`, require the `Evidence` section to carry the same set of source IDs, reconcile `sources.json[*].linked_insights` by diffing the previous and next source sets, and keep those arrays sorted and unique. Use the `03-01` duplicate-fingerprint helpers to conservatively reject only obvious duplicates: matching claim fingerprint plus overlapping source lineage, or matching title fingerprint plus identical sorted source sets. Do not add semantic embeddings, a second lineage index, report writes, stale propagation, or direct raw-URL provenance outside the insight artifact plus source ledger.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/insight-upsert.spec.ts internal/tools/researcher/__tests__/insight-lineage.spec.ts` passes with create, update, dropped-source unlink, missing-source rejection, and duplicate-prevention assertions, and `rg -n "next_ids\\.insight|inventory\\.insights|linked_insights|claimFingerprint|titleFingerprint" internal/tools/researcher/core/insights/upsert.ts internal/tools/researcher/core/insights/backlinks.ts` shows ID allocation, manifest sync, backlink reconciliation, and conservative dedupe logic.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/insight-upsert.spec.ts internal/tools/researcher/__tests__/insight-lineage.spec.ts</automated>
  </verify>
  <done>Insight promotion is deterministic, source-backed, duplicate-aware, and synchronized bidirectionally with the canonical Phase 2 source ledger.</done>
</task>

<task type="auto">
  <name>Task 2: Add the thin `research-insight` CLI entrypoint</name>
  <files>scripts/research-insight.ts, internal/tools/researcher/__tests__/insight-upsert.spec.ts</files>
  <read_first>scripts/research-source-add.ts, internal/tools/researcher/core/insights/upsert.ts, internal/tools/researcher/__tests__/insight-upsert.spec.ts</read_first>
  <action>Create `scripts/research-insight.ts` as a thin deterministic wrapper over the shared upsert core. It must accept `--project-root`, `--slug`, optional `--insight-id`, `--title`, repeated `--source-id`, `--status`, `--confidence`, repeated `--tag`, `--claim`, `--why-it-matters`, repeated `--evidence` using the exact wire format `SRC-0001::support note`, repeated `--caveat`, and `--reuse-notes`; then call the shared upsert function and print JSON only. Extend `insight-upsert.spec.ts` with a CLI smoke test that asserts stdout contains `operation`, `insightId`, `path`, and the sorted `sourceIds` list. Keep `linked_analysis` and `linked_reports` tool-managed; do not accept direct CLI flags for them, do not add interactive prompts, and do not add alternate human-only output modes.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/insight-upsert.spec.ts` passes with a CLI smoke test, and `rg -n "JSON\\.stringify|--source-id|SRC-0001::|operation|insightId|path" scripts/research-insight.ts internal/tools/researcher/__tests__/insight-upsert.spec.ts` shows the thin deterministic CLI path.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/insight-upsert.spec.ts</automated>
  </verify>
  <done>The repo exposes a runtime-neutral insight-promotion entrypoint that delegates all state mutation to shared core tooling and returns deterministic machine-readable results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/operator insight payload -> shared insight core | Untrusted titles, claim text, evidence bullets, and source IDs cross into ID allocation and durable Markdown creation. |
| insight forward lineage -> `sources.json[*].linked_insights` | One canonical source ledger must stay synchronized with every create/update of an insight artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | T | `internal/tools/researcher/core/insights/upsert.ts`, `internal/tools/researcher/core/insights/backlinks.ts` | mitigate | Validate every `SRC-*` reference against `sources.json`, reuse the shared artifact parser, diff old vs. new source sets, and reconcile sorted unique `linked_insights` arrays in the same deterministic write path. |
| T-03-02A | R | `manifest.next_ids.insight`, `insights/<INS-ID>-<slug>.md` | mitigate | Run duplicate checks before allocation, preserve the `INS-*` ID as authoritative on updates and renames, and prove create-vs-update counter behavior in `insight-upsert.spec.ts`. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/insight-upsert.spec.ts internal/tools/researcher/__tests__/insight-lineage.spec.ts`
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `rg -n "linked_insights|next_ids\\.insight|inventory\\.insights|claimFingerprint|sourceIds" internal/tools/researcher/core/insights/upsert.ts internal/tools/researcher/core/insights/backlinks.ts scripts/research-insight.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- New insights allocate one stable `INS-*` ID from the manifest-backed counter
- `sources.json[*].linked_insights` is kept bidirectionally synchronized with `derived_from_sources`
- Insight promotion rejects missing-source and obvious duplicate cases before durable writes
- The insight CLI remains thin and JSON-only
</success_criteria>

<output>
After completion, create `.planning/phases/03-insights-analysis-graph/03-02-SUMMARY.md`
</output>
