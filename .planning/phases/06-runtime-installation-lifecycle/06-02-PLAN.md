---
phase: 06-runtime-installation-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
files_modified:
  - internal/tools/researcher/runtime/adapters/claude.ts
  - internal/tools/researcher/runtime/adapters/codex.ts
  - internal/tools/researcher/runtime/payload.ts
  - internal/tools/researcher/runtime/settings.ts
  - internal/tools/researcher/runtime/templates.ts
  - internal/tools/researcher/__tests__/runtime-adapters.spec.ts
autonomous: true
requirements:
  - INST-01
  - INST-02
user_setup: []
must_haves:
  truths:
    - "Codex and Claude runtime assets are rendered from one shared command and payload catalog rather than authored independently."
    - "Adapter output is deterministic and runtime-native: Codex gets `.codex/skills/research-*`, Claude gets `.claude/commands/*.md` plus only minimal supporting config."
    - "Claude settings integration remains merge-based and non-destructive; hook behavior is optional, not implicit."
  artifacts:
    - "internal/tools/researcher/runtime/adapters/codex.ts and internal/tools/researcher/runtime/adapters/claude.ts render managed assets from the shared catalog."
    - "internal/tools/researcher/runtime/settings.ts centralizes safe settings-merge logic rather than scattering JSON edits across installer code."
    - "internal/tools/researcher/__tests__/runtime-adapters.spec.ts proves runtime parity, deterministic rendering, and non-destructive settings behavior."
  key_links:
    - "Adapter renderers consume only the shared runtime contract and catalog from Plan 01."
    - "Runtime-specific strings and file layouts are isolated inside adapter code, not spread into lifecycle services."
    - "Settings merge helpers emit explicit ownership records that later install/update/inspect can report."
---

<objective>
Build the Codex and Claude Code adapter layer that turns the shared runtime catalog into
runtime-native managed assets.

Purpose: Implement D-72 through D-78 so Researcher exposes one coherent command surface across
both runtimes while respecting their native file layouts and config semantics.
Output: Codex and Claude adapter renderers, settings merge helpers, and adapter-focused tests
aligned with `06-VALIDATION.md`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-runtime-installation-lifecycle/06-CONTEXT.md
@.planning/phases/06-runtime-installation-lifecycle/06-RESEARCH.md
@.planning/phases/06-runtime-installation-lifecycle/06-VALIDATION.md
@.planning/phases/06-runtime-installation-lifecycle/06-01-PLAN.md
@researcher/WORKFLOWS.md
@.claude/commands/create_plan.md
@.claude/settings.local.json
@go-research/external_code/get-shit-done/bin/install.js
@internal/tools/researcher/runtime/catalog.ts
@internal/tools/researcher/runtime/manifest.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render deterministic Codex and Claude runtime assets from the shared catalog</name>
  <files>internal/tools/researcher/runtime/adapters/codex.ts, internal/tools/researcher/runtime/adapters/claude.ts, internal/tools/researcher/runtime/templates.ts, internal/tools/researcher/__tests__/runtime-adapters.spec.ts</files>
  <behavior>
    - Test 1: Codex adapter renders one `SKILL.md` tree per Researcher command with deterministic names and content.
    - Test 2: Claude adapter renders the equivalent `.md` command files from the same catalog without losing descriptions or next-step guidance.
    - Test 3: both adapters expose the same conceptual command inventory and reject incomplete catalog entries.
  </behavior>
  <action>Create adapter renderers for Codex and Claude Code that consume the shared runtime catalog plus payload map and emit runtime-native managed assets. Keep all runtime-specific filename, directory, and wrapper formatting inside these adapters. The rendered runtime assets must carry enough metadata for install/update/inspect to report both their own files and the shared payload files, including schemas, that they depend on. Do not duplicate command definitions across runtimes or hand-maintain separate source trees of installed assets.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/runtime-adapters.spec.ts` passes with explicit coverage for Codex skill generation, Claude command generation, and parity of the shared command inventory.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/runtime-adapters.spec.ts</automated>
  </verify>
  <done>Researcher can now generate deterministic runtime-native assets for both supported runtimes from one source of truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add non-destructive Claude settings merge support and optional hook intent recording</name>
  <files>internal/tools/researcher/runtime/settings.ts, internal/tools/researcher/runtime/adapters/claude.ts, internal/tools/researcher/__tests__/runtime-adapters.spec.ts</files>
  <behavior>
    - Test 1: merging into an existing `.claude/settings.local.json` preserves unrelated user-owned keys.
    - Test 2: installer-required Researcher settings can be added or refreshed without overwriting unmanaged content.
    - Test 3: optional hook entries are represented explicitly in the managed asset model and are absent from the default install path.
  </behavior>
  <action>Add a dedicated Claude settings merge helper that operates surgically on the existing project-local config surface and records any managed changes in the runtime ownership model. Keep hook support explicit and optional, not part of the default adapter output. Do not overwrite entire settings files or invent a fallback config path.</action>
  <acceptance_criteria>`rg -n "settings|hook|merge" internal/tools/researcher/runtime/settings.ts internal/tools/researcher/runtime/adapters/claude.ts` shows one merge path, and `pnpm exec vitest run internal/tools/researcher/__tests__/runtime-adapters.spec.ts` passes.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/runtime-adapters.spec.ts</automated>
  </verify>
  <done>The adapter layer supports safe Claude configuration integration without clobbering user-owned runtime config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared runtime catalog -> runtime-native assets | Missing or divergent adapter mappings could create runtime drift where Codex and Claude install different products. |
| existing `.claude/settings.local.json` -> managed settings merge | Installer-owned updates must not overwrite unrelated user configuration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | T | Codex and Claude adapter renderers | mitigate | Render both runtimes from one shared catalog and test parity of command inventory and descriptions. |
| T-06-03 | T | Claude settings merge helper | mitigate | Merge only Researcher-owned keys/sections and preserve unmanaged content exactly. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/runtime-adapters.spec.ts`
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `rg -n "research-|settings|hook" internal/tools/researcher/runtime/adapters/codex.ts internal/tools/researcher/runtime/adapters/claude.ts internal/tools/researcher/runtime/settings.ts`
</verification>

<output>
After completion, create `.planning/phases/06-runtime-installation-lifecycle/06-02-SUMMARY.md`
</output>
