---
phase: 03-insights-analysis-graph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - researcher/templates/INSIGHT.md
  - researcher/templates/ANALYSIS.md
  - researcher/schemas/insight-frontmatter.schema.json
  - researcher/schemas/analysis-frontmatter.schema.json
  - internal/tools/researcher/contracts/insights.ts
  - internal/tools/researcher/contracts/analysis.ts
  - internal/tools/researcher/contracts/validators.ts
  - internal/tools/researcher/core/artifacts/markdown.ts
  - internal/tools/researcher/core/insights/dedupe.ts
  - internal/tools/researcher/__tests__/insight-contract.spec.ts
  - internal/tools/researcher/__tests__/analysis-contract.spec.ts
autonomous: true
requirements:
  - INS-01
  - INS-02
  - INS-03
  - INS-04
user_setup: []
must_haves:
  truths:
    - "Insight artifacts have stable `INS-*` identity, fixed Markdown sections, and schema-validated frontmatter before any write reaches disk."
    - "Analysis artifacts have stable `ANL-*` identity, fixed Markdown sections, explicit insight lineage inputs, and first-class contradiction, caveat, and open-question sections."
    - "One shared parser rejects malformed frontmatter, missing sections, section-order drift, and frontmatter/body lineage mismatches before later upsert flows reuse the contract."
  artifacts:
    - "researcher/schemas/insight-frontmatter.schema.json and researcher/schemas/analysis-frontmatter.schema.json freeze the Phase 3 frontmatter contracts for canonical Markdown artifacts."
    - "internal/tools/researcher/contracts/insights.ts and internal/tools/researcher/contracts/analysis.ts export typed frontmatter, parsed-document, and status interfaces for later upsert flows."
    - "internal/tools/researcher/core/artifacts/markdown.ts parses and validates canonical insight and analysis Markdown documents, and internal/tools/researcher/core/insights/dedupe.ts exposes deterministic fingerprint inputs."
  key_links:
    - "internal/tools/researcher/contracts/validators.ts compiles the new artifact schemas through the existing Ajv boundary instead of introducing a parallel validator path."
    - "internal/tools/researcher/__tests__/insight-contract.spec.ts proves `derived_from_sources` stays equal to the `Evidence` section source IDs and that duplicate-fingerprint normalization is deterministic."
    - "internal/tools/researcher/__tests__/analysis-contract.spec.ts proves `Question`, `Synthesis`, `Contradictions`, `Caveats`, `Open Questions`, and `Next Moves` remain required and ordered."
---

<objective>
Freeze the Phase 3 insight and analysis artifact contracts before any promotion or synthesis writer mutates a research workspace.

Purpose: Implement D-30 through D-40 and D-42 as one Markdown-first contract boundary so later plans reuse the same parser, schema, and fingerprint rules instead of inventing parallel artifact logic.
Output: Insight and analysis templates, frontmatter schemas, typed contract helpers, one shared Markdown parse/validate boundary, deterministic duplicate-fingerprint helpers, and contract tests aligned with `03-VALIDATION.md`.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-insights-analysis-graph/03-CONTEXT.md
@.planning/phases/03-insights-analysis-graph/03-RESEARCH.md
@.planning/phases/03-insights-analysis-graph/03-VALIDATION.md
@.planning/phases/02-source-registry-evidence-capture/02-01-SUMMARY.md
@.planning/phases/02-source-registry-evidence-capture/02-02-SUMMARY.md
@.planning/phases/02-source-registry-evidence-capture/02-03-SUMMARY.md
@researcher/README.md
@researcher/ARTIFACT-MODEL.md
@researcher/WORKFLOWS.md
@researcher/DESIGN.md
@researcher/IMPLEMENTATION-ROADMAP.md
@researcher/templates/INSIGHT.md
@internal/tools/researcher/contracts/manifest.ts
@internal/tools/researcher/contracts/sources.ts
@internal/tools/researcher/contracts/validators.ts
@internal/tools/researcher/core/resume.ts

<interfaces>
Existing manifest-backed counters:

```typescript
export interface NextResearchIds {
  source: number;
  insight: number;
  analysis: number;
  report: number;
  task: number;
}

export function formatArtifactId(
  kind: "source" | "insight" | "analysis" | "report" | "task",
  counter: number,
): string;
```

Current source-lineage anchor from Phase 2:

```typescript
export interface SourceRecord {
  id: string;
  linked_insights: string[];
  latest_capture_path: string | null;
  captures: SourceCapture[];
}
```

Resume already derives inventory by counting files on disk:

```typescript
type ResumeInventory = {
  sources: number;
  insights: number;
  analysis: number;
  reports: number;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the canonical insight and analysis Markdown contracts</name>
  <files>researcher/templates/INSIGHT.md, researcher/templates/ANALYSIS.md, researcher/schemas/insight-frontmatter.schema.json, researcher/schemas/analysis-frontmatter.schema.json, internal/tools/researcher/contracts/insights.ts, internal/tools/researcher/contracts/analysis.ts, internal/tools/researcher/__tests__/insight-contract.spec.ts, internal/tools/researcher/__tests__/analysis-contract.spec.ts</files>
  <read_first>.planning/phases/03-insights-analysis-graph/03-CONTEXT.md, .planning/phases/03-insights-analysis-graph/03-RESEARCH.md, .planning/phases/03-insights-analysis-graph/03-VALIDATION.md, researcher/ARTIFACT-MODEL.md, researcher/WORKFLOWS.md, researcher/templates/INSIGHT.md, internal/tools/researcher/contracts/manifest.ts, internal/tools/researcher/contracts/sources.ts</read_first>
  <behavior>
    - Test 1: a valid insight document with `INS-*` frontmatter, fixed `Claim -> Why It Matters -> Evidence -> Caveats -> Reuse Notes` sections, and matching `derived_from_sources` plus `Evidence` bullets parses successfully.
    - Test 2: a valid analysis document with `ANL-*` frontmatter, fixed `Question -> Synthesis -> Contradictions -> Caveats -> Open Questions -> Next Moves` sections, and at least two `derived_from_insights` entries parses successfully unless `transitional_scaffold: true` explicitly allows the D-40 scaffold exception.
    - Test 3: malformed IDs, invalid timestamps, missing sections, out-of-order sections, missing `SRC-*` evidence bullets, or missing contradiction/caveat/open-question sections are rejected before later upsert code can write artifacts.
  </behavior>
  <action>Per D-30 through D-40, update `researcher/templates/INSIGHT.md` so canonical insight files use YAML frontmatter with `id`, `title`, `status`, `confidence`, `derived_from_sources`, `tags`, `linked_analysis`, `linked_reports`, `created_at`, and `updated_at`, then add `researcher/templates/ANALYSIS.md` with YAML frontmatter that includes `id`, `title`, `status`, `confidence`, `derived_from_insights`, `tags`, `linked_reports`, `transitional_scaffold`, `created_at`, and `updated_at`. Create matching frontmatter schemas under `researcher/schemas/` and typed helpers in `internal/tools/researcher/contracts/insights.ts` plus `internal/tools/researcher/contracts/analysis.ts`. Keep insights atomic per D-31 by modeling exactly one `Claim` section and human-readable `Evidence` bullets keyed by `SRC-*`; keep analyses insight-first per D-38 by using `derived_from_insights` instead of raw source URLs. Reuse the Phase 2 confidence vocabulary, expose `draft`, `validated`, `disputed`, and `superseded` as the shared status vocabulary, and keep `linked_analysis` / `linked_reports` as tool-managed sorted arrays. Do not add notebook-first analysis, report-generation fields, stale propagation flags, embeddings, or provider-specific metadata in this contract plan.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/insight-contract.spec.ts internal/tools/researcher/__tests__/analysis-contract.spec.ts` passes with explicit cases for valid artifacts, invalid IDs/timestamps, missing sections, section-order drift, `derived_from_sources` vs `Evidence` mismatch, and the D-40 scaffold exception.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/insight-contract.spec.ts internal/tools/researcher/__tests__/analysis-contract.spec.ts</automated>
  </verify>
  <done>Canonical `INS-*` and `ANL-*` Markdown artifacts are fully specified in templates, schemas, and typed contracts, and the new contract tests prove invalid artifacts fail before disk mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the shared Markdown parser, validator, and duplicate-fingerprint helpers</name>
  <files>package.json, pnpm-lock.yaml, internal/tools/researcher/contracts/validators.ts, internal/tools/researcher/core/artifacts/markdown.ts, internal/tools/researcher/core/insights/dedupe.ts, internal/tools/researcher/__tests__/insight-contract.spec.ts, internal/tools/researcher/__tests__/analysis-contract.spec.ts</files>
  <read_first>package.json, internal/tools/researcher/contracts/validators.ts, internal/tools/researcher/contracts/sources.ts, researcher/schemas/sources.schema.json, researcher/templates/INSIGHT.md, internal/tools/researcher/__tests__/insight-contract.spec.ts, internal/tools/researcher/__tests__/analysis-contract.spec.ts</read_first>
  <behavior>
    - Test 1: the shared parser extracts YAML frontmatter through one library boundary, validates it against the new schemas, and returns typed section payloads for both artifact kinds.
    - Test 2: insight parsing extracts `SRC-*` IDs from `Evidence` bullets and rejects frontmatter/body drift when the parsed set differs from `derived_from_sources`.
    - Test 3: deterministic duplicate inputs are normalized from title text, `Claim` body text, and sorted source IDs so later upsert flows can reject obvious duplicates without embeddings.
  </behavior>
  <action>Install `gray-matter` and extend `internal/tools/researcher/contracts/validators.ts` so the existing Ajv boundary compiles the new insight and analysis schemas alongside manifest and sources. In `internal/tools/researcher/core/artifacts/markdown.ts`, implement one shared parse/validate/render boundary that: parses YAML frontmatter, validates metadata, enforces exact required `##` heading order, extracts section bodies, and rejects empty or missing required sections. In `internal/tools/researcher/core/insights/dedupe.ts`, implement the D-36 normalization primitives only: normalize title text, normalize `Claim` text, sort/uniq `derived_from_sources`, and return fingerprints that later create/update flows can compare. Keep the parser Markdown-first and deterministic; do not add a remark/unified AST pipeline, fallback parsing paths, notebook parsing, or a second provenance index.</action>
  <acceptance_criteria>`package.json` includes `gray-matter`, `pnpm exec vitest run internal/tools/researcher/__tests__/insight-contract.spec.ts internal/tools/researcher/__tests__/analysis-contract.spec.ts` passes, and `rg -n "gray-matter|validateInsightFrontmatter|validateAnalysisFrontmatter|parseInsightArtifact|parseAnalysisArtifact|createInsightFingerprint" package.json internal/tools/researcher/contracts/validators.ts internal/tools/researcher/core/artifacts/markdown.ts internal/tools/researcher/core/insights/dedupe.ts` shows one shared parser/validator path.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/insight-contract.spec.ts internal/tools/researcher/__tests__/analysis-contract.spec.ts</automated>
  </verify>
  <done>The repo has one deterministic Markdown parse/validate boundary and one conservative duplicate-fingerprint helper set that later insight and analysis writers can reuse directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-authored Markdown -> artifact parser | Untrusted frontmatter and section content crosses into durable knowledge artifacts. |
| parsed artifact metadata -> later upsert flows | Contract decisions from this plan become the trusted input for ID allocation, backlink sync, and report lineage later. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `internal/tools/researcher/core/artifacts/markdown.ts` | mitigate | Parse frontmatter through one library boundary, validate metadata with Ajv, enforce exact section order, and reject frontmatter/body lineage drift plus missing contradiction/caveat/open-question sections in the new contract specs. |
| T-03-01A | R | `researcher/templates/*.md`, `researcher/schemas/*.json` | mitigate | Freeze one inspectable Markdown template plus schema pair for each artifact kind and back them with tests so later writers cannot silently drift from D-30 through D-40. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/insight-contract.spec.ts internal/tools/researcher/__tests__/analysis-contract.spec.ts`
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `rg -n "derived_from_sources|linked_analysis|linked_reports|transitional_scaffold|Open Questions|Next Moves" researcher/templates/INSIGHT.md researcher/templates/ANALYSIS.md researcher/schemas/insight-frontmatter.schema.json researcher/schemas/analysis-frontmatter.schema.json`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `INS-*` and `ANL-*` artifacts are canonical Markdown with schema-validated frontmatter and fixed body sections
- Insight evidence bullets and `derived_from_sources` stay equal by contract
- Analysis artifacts require explicit contradictions, caveats, and open questions as structured sections
- Duplicate-detection inputs are deterministic, conservative, and embedding-free
</success_criteria>

<output>
After completion, create `.planning/phases/03-insights-analysis-graph/03-01-SUMMARY.md`
</output>
