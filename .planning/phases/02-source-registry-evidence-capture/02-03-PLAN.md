---
phase: 02-source-registry-evidence-capture
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - internal/tools/researcher/core/sources/store.ts
  - internal/tools/researcher/core/sources/captures.ts
  - internal/tools/researcher/core/sources/refresh.ts
  - internal/tools/researcher/__tests__/source-refresh.spec.ts
  - internal/tools/researcher/__tests__/resume.spec.ts
  - scripts/research-source-refresh.ts
autonomous: true
requirements:
  - SRC-03
  - SRC-04
user_setup: []
must_haves:
  truths:
    - "Captured evidence is stored durably under `data/` and linked back to one stable source record through root-relative refs."
    - "Refreshing a source appends new capture history without overwriting prior evidence or changing the stable `SRC-*` ID."
    - "A user can detect stale or rejected source evidence from source-layer metadata without waiting for downstream Phase 5 propagation."
  artifacts:
    - "internal/tools/researcher/core/sources/captures.ts writes append-only evidence files under `data/<bucket>/<SRC-ID>/<timestamp>/...`."
    - "internal/tools/researcher/core/sources/refresh.ts updates source freshness metadata, derives stale state from the manifest freshness window, manages side states, and advances latest capture pointers on the existing registry record."
    - "scripts/research-source-refresh.ts exposes a thin refresh CLI over the shared core."
  key_links:
    - "internal/tools/researcher/core/sources/captures.ts uses the Phase 2 workspace bucket contract so evidence files and registry refs stay in sync."
    - "internal/tools/researcher/core/sources/refresh.ts reuses the shared source store from Plan 02-02 instead of writing JSON directly."
    - "internal/tools/researcher/__tests__/source-refresh.spec.ts proves multiple refresh snapshots coexist and source side states such as `rejected`, `stale`, and `superseded` are surfaced correctly, including stale detection derived from the configured freshness window."
    - "internal/tools/researcher/__tests__/resume.spec.ts proves the richer Phase 2 source contract still round-trips through disk-only resume."
---

<objective>
Implement durable evidence capture storage and refresh handling on top of the new source registry.

Purpose: Complete Phase 2 by making evidence bytes durable under `data/`, keeping refresh append-only, and surfacing source-layer staleness without reopening provider choices or downstream lineage work.
Output: Shared capture writer, source refresh core, refresh tests, and a thin `research-source-refresh` CLI.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-source-registry-evidence-capture/02-CONTEXT.md
@.planning/phases/02-source-registry-evidence-capture/02-RESEARCH.md
@.planning/phases/01-research-workspace-bootstrap/01-04-SUMMARY.md
@researcher/ARTIFACT-MODEL.md
@researcher/WORKFLOWS.md
@internal/tools/researcher/contracts/sources.ts
@internal/tools/researcher/contracts/workspace.ts
@internal/tools/researcher/fs/workspace-paths.ts
@internal/tools/researcher/fs/write-json-atomically.ts
@internal/tools/researcher/core/sources/store.ts
@internal/tools/researcher/core/sources/add.ts

<interfaces>
Required evidence layout to preserve:

```text
data/
├── snapshots/<SRC-ID>/<timestamp>/
├── exports/<SRC-ID>/<timestamp>/
├── transcripts/<SRC-ID>/<timestamp>/
└── datasets/<SRC-ID>/<timestamp>/
```

Phase 2 source-record fields this plan must keep aligned:

```typescript
type SourceRecord = {
  id: string;
  latest_capture_path: string | null;
  captures: Array<{ kind: string; path: string; captured_at: string }>;
  accessed_at: string | null;
  last_checked_at: string | null;
  side_states: string[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement append-only capture storage under `data/`</name>
  <files>internal/tools/researcher/core/sources/store.ts, internal/tools/researcher/core/sources/captures.ts, internal/tools/researcher/__tests__/source-refresh.spec.ts</files>
  <read_first>.planning/phases/02-source-registry-evidence-capture/02-CONTEXT.md, researcher/ARTIFACT-MODEL.md, researcher/WORKFLOWS.md, internal/tools/researcher/contracts/sources.ts, internal/tools/researcher/contracts/workspace.ts, internal/tools/researcher/fs/workspace-paths.ts, internal/tools/researcher/fs/write-json-atomically.ts, internal/tools/researcher/core/sources/store.ts</read_first>
  <behavior>
    - Test 1: capturing a snapshot for `SRC-0001` writes a file under `data/snapshots/SRC-0001/<timestamp>/...`, stores a matching root-relative capture ref, and sets `latest_capture_path`.
    - Test 2: a second capture for the same source appends a new `captures[]` entry and keeps the earlier capture intact on disk.
    - Test 3: invalid capture kinds, missing source files, or capture refs outside `data/` fail before the registry is updated.
  </behavior>
  <action>Create `internal/tools/researcher/core/sources/captures.ts` per D-19 and D-21 through D-24. The shared capture writer must accept a source ID, a capture kind (`snapshot`, `export`, `transcript`, or `dataset`), a source file path outside the workspace, and a deterministic `capturedAt` timestamp; then copy the file into `data/<bucket>/<SRC-ID>/<YYYYMMDDTHHmmssZ>/<original-basename>` and return one root-relative capture ref. Reuse the Phase 2 bucket constants and capture-ref validator from Plan 02-01, and extend `store.ts` only as needed so the capture writer can stage registry updates through the same validated persistence boundary from Plan 02-02. Add `source-refresh.spec.ts` as the dedicated integration test file for append-only capture history. Do not overwrite prior capture files, inline file contents into `sources.json`, or write outside `data/`.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/source-refresh.spec.ts` passes with assertions for timestamped `data/<bucket>/<SRC-ID>/...` paths and preserved prior captures, and `rg -n "snapshots|exports|transcripts|datasets|latest_capture_path|captures" internal/tools/researcher/core/sources/captures.ts internal/tools/researcher/__tests__/source-refresh.spec.ts` shows append-only evidence handling.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/source-refresh.spec.ts</automated>
  </verify>
  <done>Evidence capture is durable, append-only, attributable to one source ID, and stored under the locked `data/` buckets with root-relative refs back into `sources.json`.</done>
</task>

<task type="auto">
  <name>Task 2: Implement source refresh metadata updates and the thin refresh CLI</name>
  <files>internal/tools/researcher/core/sources/refresh.ts, scripts/research-source-refresh.ts, internal/tools/researcher/__tests__/source-refresh.spec.ts, internal/tools/researcher/__tests__/resume.spec.ts</files>
  <read_first>internal/tools/researcher/core/sources/store.ts, internal/tools/researcher/core/sources/captures.ts, scripts/research-source-add.ts, internal/tools/researcher/__tests__/source-refresh.spec.ts, internal/tools/researcher/__tests__/resume.spec.ts</read_first>
  <behavior>
    - Test 1: refreshing a source with no recent `last_checked_at`, `accessed_at`, or fresh capture relative to `manifest.freshness.window_days` derives `stale` automatically and increments freshness debt without requiring `--mark-stale`.
    - Test 2: refreshing the same source with a new capture or explicit check timestamp inside the freshness window clears derived `stale`, preserves one `SRC-*` ID, and keeps prior captures intact.
    - Test 3: manual side-state toggles for `rejected` and `superseded` still work alongside the derived stale rule, and resume remains compatible with the richer source schema.
  </behavior>
  <action>Create `internal/tools/researcher/core/sources/refresh.ts` per D-24 through D-29. The refresh core must locate an existing source by `sourceId` or canonical URL, preserve the stable `SRC-*` ID, optionally append a new capture through `captures.ts`, update `accessed_at`, `last_checked_at`, `updated_at`, `latest_capture_path`, and `captures`, and manage source-layer side states so `rejected`, `stale`, and `superseded` can coexist with the primary workflow status from D-17. During every refresh, derive the `stale` side state deterministically from `manifest.freshness.window_days` by comparing the operation timestamp against the newest trusted freshness signal on the source record (`last_checked_at`, then `accessed_at`, then the latest capture timestamp); add `stale` when the source is outside the freshness window and clear derived `stale` when a new check or capture brings it back inside the window. Keep `--mark-stale` and `--clear-stale` only as explicit operator overrides, layered after the automatic rule. Compute `manifest.freshness.last_source_sync_at` from the refresh operation timestamp and set `manifest.freshness.debt` to the count of source records whose final `side_states` include `stale`, per D-26. Add `scripts/research-source-refresh.ts` as a thin CLI that accepts `--project-root`, `--slug`, and either `--source-id` or `--canonical-url`, plus optional `--status`, `--accessed-at`, `--last-checked-at`, `--capture-kind`, `--capture-file`, `--captured-at`, `--mark-stale`, `--clear-stale`, `--mark-superseded`, `--clear-superseded`, `--mark-rejected`, and `--clear-rejected`, then prints deterministic JSON only. Extend `source-refresh.spec.ts` with refresh-flow and CLI smoke coverage, including automatic stale detection from the freshness window plus explicit rejected-side-state mutation, and extend `resume.spec.ts` with a Phase 2 compatibility case proving disk-only resume still reports correct source inventory and next action after the richer source schema and refresh metadata land. Do not fetch URLs, overwrite existing capture files in place, or add downstream stale-impact propagation to insights, analysis, or reports.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/source-refresh.spec.ts internal/tools/researcher/__tests__/resume.spec.ts` passes with assertions that refresh preserves one `SRC-*` ID, appends capture history, derives `stale` from `manifest.freshness.window_days`, updates `rejected`, `stale`, and `superseded` side states, recomputes `manifest.freshness.debt`, and leaves resume compatible with the richer source contract, and `rg -n "window_days|mark-stale|clear-stale|mark-superseded|clear-superseded|mark-rejected|clear-rejected|last_source_sync_at|freshness\\.debt|nextRecommendedAction|JSON\\.stringify" internal/tools/researcher/core/sources/refresh.ts scripts/research-source-refresh.ts internal/tools/researcher/__tests__/resume.spec.ts` shows the refresh, automatic stale-detection, CLI, and resume-compatibility flow.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/source-refresh.spec.ts internal/tools/researcher/__tests__/resume.spec.ts</automated>
  </verify>
  <done>The repo exposes an append-only refresh path that keeps source records stable, evidence durable, source-layer side states machine-readable, and Phase 1 resume compatibility intact after the Phase 2 schema expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external capture file -> workspace `data/` tree | Arbitrary file input crosses into durable evidence storage. |
| refresh metadata -> existing source registry record | Source-side state and freshness markers cross into already-trusted registry entries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | T | `internal/tools/researcher/core/sources/captures.ts` | mitigate | Copy captures only into validated `data/<bucket>/<SRC-ID>/<timestamp>/...` destinations and reject invalid kinds, missing files, and escape-prone refs before any registry update. |
| T-02-08 | T | `internal/tools/researcher/core/sources/refresh.ts` | mitigate | Refresh by stable source ID or canonical locator, append captures instead of overwriting them, and recompute source-layer stale debt from the persisted registry. |
| T-02-09 | R | `internal/tools/researcher/core/sources/store.ts` | mitigate | Keep evidence-file writes and registry/manifest updates inside one shared deterministic boundary so `sources.json` metadata cannot drift from on-disk evidence paths unnoticed. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/source-refresh.spec.ts`
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/resume.spec.ts`
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `rg -n "data/(snapshots|exports|transcripts|datasets)|freshness\\.debt|latest_capture_path" internal/tools/researcher/core/sources/captures.ts internal/tools/researcher/core/sources/refresh.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Capture files are stored under `data/` with root-relative refs in `sources.json`
- Refresh preserves one stable source record and appends new capture history instead of overwriting prior evidence
- Source-layer `rejected`, `stale`, and `superseded` states are machine-readable through source side states, with `stale` derived from the manifest freshness window during refresh and manifest freshness debt reflecting stale-source count
- Resume remains compatible with the enriched Phase 2 source contract and refresh metadata
- The refresh CLI remains thin and provider-neutral
</success_criteria>

<output>
After completion, create `.planning/phases/02-source-registry-evidence-capture/02-03-SUMMARY.md`
</output>
