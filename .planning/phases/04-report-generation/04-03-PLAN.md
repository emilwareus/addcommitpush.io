---
phase: 04-report-generation
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - internal/tools/researcher/core/reports/lineage.ts
  - internal/tools/researcher/core/reports/upsert.ts
  - internal/tools/researcher/__tests__/report-lineage.spec.ts
  - internal/tools/researcher/__tests__/resume.spec.ts
autonomous: true
requirements:
  - RPT-03
  - RPT-02
user_setup: []
must_haves:
  truths:
    - "Every report can be traced back through explicit `ANL-*`, `INS-*`, and `SRC-*` references rendered from the on-disk artifact graph."
    - "Report lineage rendering stays explicit and terminal: the main narrative cites insights, and the closing reference sections resolve the support chain."
    - "Resume remains disk-only and recognizes report presence by routing to `review-existing-reports` once reports exist."
  artifacts:
    - "internal/tools/researcher/core/reports/lineage.ts resolves analyses, insights, and supporting sources into report-facing reference payloads."
    - "internal/tools/researcher/core/reports/upsert.ts consumes those lineage payloads when rendering canonical reports."
    - "internal/tools/researcher/__tests__/report-lineage.spec.ts and internal/tools/researcher/__tests__/resume.spec.ts prove explicit traceability and report-aware resume routing."
  key_links:
    - "lineage resolution walks the existing report -> analysis -> insight -> source chain and does not create a sidecar provenance file."
    - "source URLs appear in `Source References`, not inline in main report prose."
    - "resume remains a filesystem consumer only, even after report artifacts exist."
---

<objective>
Render explicit report lineage sections and keep resume compatible once reports are present on disk.

Purpose: Deliver RPT-03 and finish RPT-02 by making report traceability explicit and preserving
disk-only resume behavior after report packaging lands.
Output: shared report lineage resolver, lineage tests, and report-aware resume coverage.
</objective>

<context>
@.planning/phases/04-report-generation/04-CONTEXT.md
@.planning/phases/04-report-generation/04-RESEARCH.md
@.planning/phases/04-report-generation/04-VALIDATION.md
@.planning/phases/04-report-generation/04-02-PLAN.md
@researcher/REPORTING.md
@internal/tools/researcher/core/reports/upsert.ts
@internal/tools/researcher/core/artifacts/markdown.ts
@internal/tools/researcher/core/resume.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve and render analysis, insight, and source lineage into canonical report sections</name>
  <files>internal/tools/researcher/core/reports/lineage.ts, internal/tools/researcher/core/reports/upsert.ts, internal/tools/researcher/__tests__/report-lineage.spec.ts</files>
  <behavior>
    - Test 1: a report built from selected analyses and insights renders an `Analysis Inputs` section listing each `ANL-*` input.
    - Test 2: `Insight References` resolve each included insight ID to its title/claim plus supporting `SRC-*` IDs.
    - Test 3: `Source References` list each resolved source ID with title and canonical URL, deduped across overlapping insight support.
  </behavior>
  <action>Create a lineage resolver that loads referenced analyses and insights from disk through the shared parser, then walks insight support back to `sources.json`. Render `Analysis Inputs`, `Insight References`, and `Source References` deterministically from that resolved graph. Keep source URLs confined to the terminal source section, and do not create a report-only provenance registry or source-first narrative mode.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/report-lineage.spec.ts` passes with explicit coverage for analysis reference rendering, insight-to-source resolution, source dedupe, and source URL placement in the terminal reference section.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/report-lineage.spec.ts</automated>
  </verify>
  <done>Reports carry an explicit human-readable and machine-parsable support chain without collapsing back to raw-source writing.</done>
</task>

<task type="auto">
  <name>Task 2: Extend resume coverage for report-presence routing</name>
  <files>internal/tools/researcher/__tests__/resume.spec.ts</files>
  <behavior>
    - Test 1: an extract/package-stage workspace with reports on disk returns `review-existing-reports`.
    - Test 2: stale source debt still outranks report presence and returns `refresh-sources`.
  </behavior>
  <action>Extend `internal/tools/researcher/__tests__/resume.spec.ts` with a report-present fixture that uses the new report writer and confirms disk-only inventory plus next-action routing once reports exist. Keep this as coverage only unless a real bug is exposed; do not redesign resume or Phase 5 freshness propagation here.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/resume.spec.ts` passes with report-present routing coverage and stale-debt priority assertions.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/resume.spec.ts</automated>
  </verify>
  <done>Phase 4 report artifacts remain compatible with the existing disk-only resume model and next-action routing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report lineage renderer -> visible references | The rendered support chain must not misstate or drop upstream artifact lineage. |
| report-presence on disk -> resume routing | Resume must not rely on hidden state once reports exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | R | `internal/tools/researcher/core/reports/lineage.ts`, `internal/tools/researcher/core/reports/upsert.ts` | mitigate | Resolve lineage only from canonical on-disk artifacts and render explicit terminal reference sections. |
| T-04-04 | R | `internal/tools/researcher/core/resume.ts`, `internal/tools/researcher/__tests__/resume.spec.ts` | mitigate | Prove report presence and stale-source priority through disk-backed resume fixtures, not in-memory shortcuts. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/report-lineage.spec.ts`
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/resume.spec.ts`
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `rg -n "Analysis Inputs|Insight References|Source References|review-existing-reports|refresh-sources" internal/tools/researcher/core/reports/lineage.ts internal/tools/researcher/__tests__/report-lineage.spec.ts internal/tools/researcher/__tests__/resume.spec.ts`
</verification>

<output>
After completion, create `.planning/phases/04-report-generation/04-03-SUMMARY.md`
</output>
