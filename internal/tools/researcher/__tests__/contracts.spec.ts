import { describe, test } from "vitest";

describe("research workspace contract", () => {
  test.todo(
    "D-01/D-02/D-03: fixes the workspace layout at researcher/researches/<slug>/ with brief.md, manifest.json, sources.json, insights/, data/, analysis/, and reports/",
  );
  test.todo(
    "D-05/D-06/D-07: preserves a stable public sources.json contract while routing structured registry writes through deterministic tooling",
  );
  test.todo(
    "D-08/D-09: defines stable artifact identities and reserves the SRC -> INS -> ANL -> RPT lineage chain in the contract layer",
  );
  test.todo(
    "D-10: keeps human-authored artifacts markdown-first while machine state remains in structured contract files",
  );
  test.todo(
    "D-11/D-12/D-13: keeps the contract runtime-neutral and free of Codex, Claude Code, or prompt-specific control-plane details",
  );
});
