---
phase: 04-report-generation
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - internal/tools/researcher/core/reports/backlinks.ts
  - internal/tools/researcher/core/reports/upsert.ts
  - internal/tools/researcher/__tests__/report-upsert.spec.ts
  - scripts/research-report.ts
autonomous: true
requirements:
  - RPT-01
  - RPT-02
user_setup: []
must_haves:
  truths:
    - "A user can create or update canonical `RPT-*` Markdown reports from explicit audience/angle/thesis plus selected `ANL-*` and `INS-*` inputs."
    - "One research can hold multiple reports without mutating the underlying insight or analysis content, except for synchronized `linked_reports` backlinks."
    - "Report writes stay deterministic and go through one shared core path with a thin CLI wrapper."
  artifacts:
    - "internal/tools/researcher/core/reports/upsert.ts provides deterministic create/update behavior for report artifacts."
    - "internal/tools/researcher/core/reports/backlinks.ts reconciles `linked_reports` on referenced analysis and insight artifacts."
    - "scripts/research-report.ts and internal/tools/researcher/__tests__/report-upsert.spec.ts prove the runtime entrypoint and multi-report behavior."
  key_links:
    - "report upsert reuses the existing artifact parser and manifest counter model rather than inventing report-only state."
    - "report backlink reconciliation updates both analyses and insights from the report’s forward lineage."
    - "the CLI remains JSON-only and does not bypass explicit audience/angle/input requirements."
---

<objective>
Build deterministic report creation/update and backlink synchronization on top of the frozen report contract.

Purpose: Deliver RPT-01 and RPT-02 by turning explicit report requests into canonical `RPT-*`
files while preserving reuse of the upstream artifact graph.
Output: shared report upsert and backlink helpers, report upsert tests, and a thin `research-report`
CLI.
</objective>

<context>
@.planning/phases/04-report-generation/04-CONTEXT.md
@.planning/phases/04-report-generation/04-RESEARCH.md
@.planning/phases/04-report-generation/04-VALIDATION.md
@.planning/phases/04-report-generation/04-01-PLAN.md
@internal/tools/researcher/contracts/manifest.ts
@internal/tools/researcher/contracts/analysis.ts
@internal/tools/researcher/contracts/insights.ts
@internal/tools/researcher/core/artifacts/markdown.ts
@internal/tools/researcher/core/insights/upsert.ts
@internal/tools/researcher/core/analysis/upsert.ts
@internal/tools/researcher/core/sources/store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement report upsert and linked-report reconciliation</name>
  <files>internal/tools/researcher/core/reports/backlinks.ts, internal/tools/researcher/core/reports/upsert.ts, internal/tools/researcher/__tests__/report-upsert.spec.ts</files>
  <behavior>
    - Test 1: creating a new report allocates the next `RPT-*`, writes `reports/<RPT-ID>-<slug>.md`, increments `manifest.inventory.reports`, and appends the report ID to each referenced insight’s and analysis’s `linked_reports` array.
    - Test 2: creating a second report for the same research produces a new `RPT-*` without mutating the first report’s identity or deleting upstream artifacts.
    - Test 3: updating a report preserves its stable ID and reconciles removed and added insight/analysis backlinks.
  </behavior>
  <action>Create `internal/tools/researcher/core/reports/upsert.ts` and `internal/tools/researcher/core/reports/backlinks.ts` using the same deterministic create/update pattern already established for insights and analyses. Require explicit audience, angle, thesis, and at least one upstream `ANL-*` or `INS-*` input. Keep report writes under `reports/`, preserve upstream artifact content except for `linked_reports` reconciliation, and sync manifest report inventory plus `next_ids.report`. Do not add source-layer report backlinks or stale propagation behavior in this plan.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/report-upsert.spec.ts` passes with create, multi-report coexistence, update reconciliation, and backlink sync coverage.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/report-upsert.spec.ts</automated>
  </verify>
  <done>Canonical report artifacts can be created and updated deterministically from explicit upstream artifact selections, and both analyses and insights know which reports reuse them.</done>
</task>

<task type="auto">
  <name>Task 2: Add the thin `research-report` runtime entrypoint</name>
  <files>scripts/research-report.ts, internal/tools/researcher/__tests__/report-upsert.spec.ts</files>
  <behavior>
    - Test 1: the CLI accepts explicit audience, angle, thesis, repeated `--analysis-id`, repeated `--insight-id`, and optional `--report-id`, then prints deterministic JSON.
    - Test 2: missing required request fields fail hard rather than producing partial reports.
  </behavior>
  <action>Add `scripts/research-report.ts` as a JSON-only wrapper over `upsertReport()`. It should accept `--project-root`, `--slug`, optional `--report-id`, `--title`, `--audience`, `--angle`, `--thesis`, repeated `--analysis-id`, repeated `--insight-id`, repeated `--key-point`, `--summary`, `--body`, repeated `--limitation`, and optional `--status`. Keep all writing logic in the shared core. Do not embed fallback prompting, interactive selection, or source resolution logic in the CLI.</action>
  <acceptance_criteria>`pnpm exec vitest run internal/tools/researcher/__tests__/report-upsert.spec.ts` passes with CLI smoke coverage and deterministic JSON assertions.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/report-upsert.spec.ts</automated>
  </verify>
  <done>Report generation has a runtime-neutral entrypoint that stays thin and deterministic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report request -> shared report core | Untrusted audience/angle/thesis/input selections cross into durable report files and backlink mutations. |
| report forward lineage -> analysis/insight backlinks | Report references must not corrupt upstream `linked_reports` arrays or leave stale backlinks behind. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | T | `internal/tools/researcher/core/reports/upsert.ts`, `internal/tools/researcher/core/reports/backlinks.ts` | mitigate | Require existing upstream IDs, keep report identity stable on update, and reconcile `linked_reports` from the report’s forward lineage only. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/report-upsert.spec.ts`
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `rg -n "next_ids\\.report|inventory\\.reports|linked_reports|JSON\\.stringify" internal/tools/researcher/core/reports/upsert.ts internal/tools/researcher/core/reports/backlinks.ts scripts/research-report.ts`
</verification>

<output>
After completion, create `.planning/phases/04-report-generation/04-02-SUMMARY.md`
</output>
