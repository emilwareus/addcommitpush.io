---
phase: 01-research-workspace-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - vitest.config.ts
  - internal/tools/researcher/__tests__/test-helpers.ts
  - internal/tools/researcher/__tests__/contracts.spec.ts
  - internal/tools/researcher/__tests__/init.spec.ts
  - internal/tools/researcher/__tests__/resume.spec.ts
autonomous: true
requirements:
  - RSCH-01
  - RSCH-03
user_setup: []
must_haves:
  truths:
    - "Phase 1 can be implemented against a stable, executable verification loop instead of informal plan text."
    - "The contract, init, and resume behaviors are pinned in reusable spec files before implementation changes land."
    - "Phase 1 verification is repo-standardized through `pnpm lint`, `pnpm typecheck`, and focused Vitest runs."
  artifacts:
    - "package.json exposes the repo-required `typecheck` script and Researcher test dependencies."
    - "vitest.config.ts scopes the test runner to `internal/tools/researcher/__tests__/**/*.spec.ts`."
    - "`internal/tools/researcher/__tests__/contracts.spec.ts`, `init.spec.ts`, and `resume.spec.ts` exist as Wave 0 feedback inputs."
  key_links:
    - "package.json -> vitest.config.ts keeps Researcher verification scriptable without app-dev workflows."
    - "internal/tools/researcher/__tests__/test-helpers.ts provides shared temp-workspace setup for all later filesystem specs."
    - "01-VALIDATION.md can point at real spec files from Wave 1 onward instead of referencing future files."
---

<objective>
Establish the Wave 0 verification harness and spec scaffolds that all later Phase 1 implementation must satisfy.

Purpose: Fix the Nyquist loop first so contract, init, and resume work can be implemented against executable feedback instead of drifting plan text.
Output: Root test dependencies, repo-required `typecheck`, Researcher Vitest config, shared test helpers, and spec files for contract, init, and resume behavior.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-research-workspace-bootstrap/01-CONTEXT.md
@.planning/phases/01-research-workspace-bootstrap/01-RESEARCH.md
@.planning/phases/01-research-workspace-bootstrap/01-VALIDATION.md
@package.json
@tsconfig.json

<interfaces>
Wave 0 spec files that must exist after this plan:

```text
internal/tools/researcher/__tests__/contracts.spec.ts
internal/tools/researcher/__tests__/init.spec.ts
internal/tools/researcher/__tests__/resume.spec.ts
```

Repo verification contract to preserve:

```text
pnpm lint
pnpm typecheck
pnpm exec vitest run internal/tools/researcher/__tests__/contracts.spec.ts internal/tools/researcher/__tests__/init.spec.ts internal/tools/researcher/__tests__/resume.spec.ts
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Researcher test harness and repo-required typecheck script</name>
  <files>package.json, pnpm-lock.yaml, vitest.config.ts, internal/tools/researcher/__tests__/test-helpers.ts</files>
  <read_first>package.json, tsconfig.json, .planning/phases/01-research-workspace-bootstrap/01-VALIDATION.md</read_first>
  <action>Install `ajv` and `vitest`, add the repo-required `typecheck` script as `tsc --noEmit`, create a root `vitest.config.ts` that only targets `internal/tools/researcher/__tests__/**/*.spec.ts`, and add shared temp-directory helpers under `internal/tools/researcher/__tests__/test-helpers.ts`. Keep the harness runtime-neutral per D-11 through D-13: do not add plan-specific script aliases beyond `typecheck`, do not couple to Next.js routes, and do not introduce watch-mode defaults.</action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm exec vitest run --config vitest.config.ts --passWithNoTests</automated>
  </verify>
  <done>The repo can run Researcher-focused tests through Vitest and script-based typechecking without relying on app-dev workflows.</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 contract, init, and resume spec scaffolds</name>
  <files>internal/tools/researcher/__tests__/contracts.spec.ts, internal/tools/researcher/__tests__/init.spec.ts, internal/tools/researcher/__tests__/resume.spec.ts</files>
  <read_first>.planning/phases/01-research-workspace-bootstrap/01-CONTEXT.md, .planning/phases/01-research-workspace-bootstrap/01-RESEARCH.md, .planning/phases/01-research-workspace-bootstrap/01-VALIDATION.md</read_first>
  <action>Create executable spec files for the contract, init, and resume surfaces before implementation lands. Encode the required Phase 1 behaviors from D-01 through D-13 as `test.todo` or skipped test scaffolds so the files exist immediately, the quick-run command is valid from Wave 1 onward, and later plans can replace the placeholders with real assertions instead of inventing behavior ad hoc. Keep the specs filesystem-focused and runtime-neutral.</action>
  <verify>
    <automated>pnpm exec vitest run internal/tools/researcher/__tests__/contracts.spec.ts internal/tools/researcher/__tests__/init.spec.ts internal/tools/researcher/__tests__/resume.spec.ts</automated>
  </verify>
  <done>The Nyquist loop is executable from Wave 1 onward because all Phase 1 spec files exist and express the required behaviors up front.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan expectations -> automated test files | The phase's intended behavior becomes executable verification inputs that later plans will trust. |
| package scripts -> repository verification | Root scripts and config determine whether later plans can prove their changes without ad hoc commands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `internal/tools/researcher/__tests__/*.spec.ts` | mitigate | Create Wave 0 spec files immediately so later plans cannot silently change behavior outside executable tests. |
| T-01-02 | D | `package.json` / `vitest.config.ts` | mitigate | Standardize on `pnpm lint`, `pnpm typecheck`, and a focused Vitest config so verification cannot drift into one-off commands. |
| T-01-03 | R | `internal/tools/researcher/__tests__/test-helpers.ts` | accept | Test helpers only manage temp directories and do not cross a production trust boundary in Phase 1. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint`
- [ ] `pnpm typecheck`
- [ ] `pnpm exec vitest run internal/tools/researcher/__tests__/contracts.spec.ts internal/tools/researcher/__tests__/init.spec.ts internal/tools/researcher/__tests__/resume.spec.ts`
- [ ] `rg -n "\"typecheck\"|internal/tools/researcher/__tests__/.*\\.spec\\.ts" package.json vitest.config.ts .planning/phases/01-research-workspace-bootstrap/01-VALIDATION.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Wave 0 creates the three spec files referenced by the phase validation contract
- The phase now verifies through `pnpm lint`, `pnpm typecheck`, and focused Vitest runs
- Later plans can activate existing spec files instead of introducing new feedback surfaces mid-phase
</success_criteria>

<output>
After completion, create `.planning/phases/01-research-workspace-bootstrap/01-01-SUMMARY.md`
</output>
